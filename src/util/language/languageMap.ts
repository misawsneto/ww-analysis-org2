/**
 * Backwards-compatible language presentation exports.
 * Canonical values are owned by the config-level language registry.
 */
export {
  LANGUAGE_DISPLAY_NAMES,
  SPECIAL_FILENAMES,
  getLanguageDisplayName,
  getLanguageIconFile,
} from "@src/config/languageMap";
