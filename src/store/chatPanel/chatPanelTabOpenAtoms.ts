/**
 * Chat-panel tab open atoms — re-exported from `chatPanelTabOpen/` so every
 * existing `@src/store/chatPanel/chatPanelTabOpenAtoms` import keeps resolving.
 *
 * Source files:
 *   chatPanelTabOpen/startPage.ts      — Launchpad + Explore
 *   chatPanelTabOpen/session.ts        — session, terminal, runtime, run group
 *   chatPanelTabOpen/workManagement.ts — Work tab, workspace, org, item, project
 *   chatPanelTabOpen/integrations.ts   — Team Inbox, GitHub issue/PR, channels
 */
export * from "./chatPanelTabOpen";
