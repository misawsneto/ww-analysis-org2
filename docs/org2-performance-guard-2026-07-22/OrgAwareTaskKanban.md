# Performance Guard — Organization-aware Task Kanban

| Area               | Verdict | Evidence                                                                                                                                                                                                                                                                             | Change or reason kept                                                                                                                                                                                                                        | Verification                                                                                                                                                                                    |
| ------------------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Background work    | fix     | Task Kanban's prior 30-second interval ran while the document was hidden                                                                                                                                                                                                             | Replaced it with one recursive timeout that pauses hidden, refreshes once on visibility return, and removes its listener/timer on unmount                                                                                                    | `useKanbanNowTick.test.ts` checks one timer, hidden pause, return refresh, and disposal                                                                                                         |
| Memory             | fix     | Every column previously exposed its full task set to the card renderer; `useSessionImpact` still invokes the unscoped Orgtrack summary command, which materializes all summary/final-diff/commit-link rows. The inspected database has 5,144 Orgtrack sessions and 1,113 final diffs | Columns now reveal 25 cards initially and 25 per distinct bottom reach; revealed ranges above 25 stay virtualized. The full task metadata and the unscoped Orgtrack summary result remain retained and require a separate scoped-data change | `taskRenderWindow.test.ts` covers 25-row growth/clamping; current dev Tauri footprint was ~790 MB (1.0 GB peak), while the separately named webpack process was ~9.1 GB (11 GB peak)            |
| Scope/isolation    | keep    | Remote roster entries are keyed by auth identity and selected cloud org and cleared on identity switch                                                                                                                                                                               | Reuses `useCloudOrgRemoteSessions`; does not persist teammate metadata into `sessionsAtom` or cross Personal/local-org scopes                                                                                                                | Projection tests cover owner-aware collisions and imported-copy dedupe                                                                                                                          |
| Rendering/hot path | fix     | Up to 40 cards per column previously used the static path, allowing roughly 200 card/DnD hook trees across five columns before virtualization began                                                                                                                                  | Initial card mounts are capped at 25 per column; subsequent pages use the existing TanStack virtualizer. Scroll loading is event-driven, contains no timer/listener, and locks one reveal to each content height                             | Five focused tests pass; changed-file ESLint and full TypeScript typecheck pass. Live long-column interaction was not targetable because the dev and installed apps share one bundle identifier |

Lifecycle matrix checked: app mount/unmount, visible/hidden/focus return, Personal/local/cloud scope, sign-out/account switch, org switch with open detail, remote Realtime invalidation/TTL revalidation, imported replay insertion, column scroll growth, and repeated scroll events at one content height. Network retry, secondary-instance data homes, workers, and deletion add no new resource beyond the shared roster hook.

**Performance verdict: fail** — the new card-render window is bounded, but Task Kanban still loads the full sidebar metadata batch on mount and retains an unscoped all-history Orgtrack summary result. The Diary view also fans out artifact and Git queries across all scoped tasks/repos before applying its selected-day presentation.
