# App memory monitor frontend UI audit

The configured `frontend-ui-audit` skill file was not available in this
worktree or the documented user-global location. This report applies the
repository's required table format manually to the changed monitor surfaces.

| Line                                                    | Element                              | Verdict          | Reason                                                                                                                                                                             | Suggested change                                                                                                 |
| ------------------------------------------------------- | ------------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `SidebarRamMonitorButton/index.tsx:45`                  | App total selector                   | abstract         | Sidebar and Settings previously implemented different sums. A shared selector now makes the product boundary identical.                                                            | Implemented: use `getAppMemoryTotals`.                                                                           |
| `SidebarRamMonitorButton/index.tsx:192`                 | Primary App memory row               | fix              | The old value was backend RSS plus heuristic helpers. It now renders the native effective total from the shared snapshot.                                                          | Implemented.                                                                                                     |
| `SidebarRamMonitorButton/index.tsx:202`                 | Measurement row                      | fix              | Users need to distinguish native, compatibility, mixed, fallback, and unavailable values.                                                                                          | Implemented with localized labels.                                                                               |
| `SidebarRamMonitorButton/index.tsx`                     | Partial-attribution notice (removed) | fix              | The always-on warning competed with the primary metric and made both compact surfaces visually noisy. Attribution still remains in the wire model so unsafe helpers stay excluded. | Implemented: omit the visible banner from Sidebar and Settings.                                                  |
| `SidebarRamMonitorButton/MemoryBreakdownSection.tsx:21` | Advanced diagnostics disclosure      | keep with reason | Existing disclosure prevents RSS, file-cache, DOM, canvas, and runtime estimates from competing with the primary native value.                                                     | Keep App backend and owned helpers visible; keep diagnostics collapsed by default.                               |
| `MonitorSection.tsx:244`                                | Resource summary                     | keep with reason | Existing `SectionContainer`, `SectionRow`, and `ProgressBar` match the Settings design system and accessibility structure.                                                         | Keep.                                                                                                            |
| `MonitorSection.tsx:262`                                | Measurement and error state          | fix              | The former UI presented every value as RAM without exposing its metric.                                                                                                            | Implemented with a localized metric row and the existing error state; no partial-attribution banner is rendered. |
| `MonitorSection.tsx:284`                                | Memory breakdown table               | keep with reason | Existing `SettingsTable` provides consistent columns, sorting, spacing, and row semantics.                                                                                         | Keep; only native App members plus the explicitly labeled RSS diagnostic are shown.                              |
| `MonitorSection.tsx:306`                                | Tool process diagnostics             | fix              | Terminal/CLI/MCP/tool rows were previously mixed into the top total and helper section.                                                                                            | Implemented as a separate RSS-only section that says it is excluded from App memory.                             |
| `appMemorySnapshot.ts:186`                              | Shared polling lifecycle             | abstract         | Two independent pollers could show different totals and duplicate native scans.                                                                                                    | Implemented one visibility-aware store and one in-flight RPC.                                                    |

Verdict counts: **fix 5**, **keep with reason 3**, **abstract 2**. No remaining
multi-file design-system sweep candidate was found in the changed surface.
