# Project organization trail select

## Scope

Audited the create-project organization trail, the floating Project and Work Item properties panes, and property-row parity with the focused-chat Workstation environment trail.

## Findings

| Line                                                                             | Element                           | Verdict          | Reason                                                                                                                                                                                                                          | Suggested change                                                                               |
| -------------------------------------------------------------------------------- | --------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `src/modules/shared/layouts/blocks/WorkstationTrailSurface.tsx:17`               | Shared Workstation trail surface  | abstract         | The focused-chat environment trail, create-project organization selector, and complete properties pane now consume the same editor-canvas background, border, padding, radius, and dropdown shadow.                             | Keep environment-style floating rails and panels on this exact surface.                        |
| `src/modules/shared/layouts/blocks/WorkstationTrailSurface.tsx:21`               | Shared Workstation trail header   | abstract         | The environment and properties trails share the same 28px title row, 4px content gap, title inset and typography, plus the same 26px collapse-button geometry.                                                                  | Compose trail titles and collapse actions from the shared header and icon button.              |
| `src/modules/shared/layouts/blocks/WorkstationTrailSurface.tsx:68`               | Shared Workstation trail body     | abstract         | Environment, Project properties, and Work Item properties now use one direct scroll body rather than rebuilding nested min-height and overflow containers.                                                                      | Keep trail content directly inside this body.                                                  |
| `src/modules/ProjectManager/shared/components/ProjectOrganizationSelect.tsx:23`  | Workstation organization selector | abstract         | One component owns the create trail's organization icon, trail typography, floating surface, dropdown sizing, search behavior, loading state, and accessible name.                                                              | Keep future organization selectors placed directly in Workstation trails on this component.    |
| `src/modules/ProjectManager/shared/components/ProjectOrganizationField.tsx:30`   | Properties organization field     | abstract         | Organization now composes the same `FieldRow`, `SearchableDropdown`, and `Option` primitives as Status instead of embedding a differently formatted Select in the pane.                                                         | Use this field wherever an editable organization belongs inside a properties panel.            |
| `src/modules/ProjectManager/shared/components/PropertiesPanel/index.tsx:31`      | Content-hugging properties shell  | abstract         | The shared shell can now hug its rows while retaining max-height scrolling; existing docked property panels keep their fill-height default.                                                                                     | Use `fitContent` only when the surrounding surface is intentionally floating.                  |
| `src/modules/ProjectManager/shared/components/PropertiesPanel/index.tsx:51`      | Properties title-row actions      | abstract         | The shared 40px properties title row now accepts trailing actions, so floating panes can use the same collapse affordance without rebuilding the header geometry.                                                               | Keep pane-local collapse controls in `headerActions`.                                          |
| `src/modules/ProjectManager/shared/components/ProjectOrganizationSelect.tsx:49`  | Workstation-trail dimensions      | keep with reason | The 24px trigger, 13px label, 14px icon, and 1px horizontal inset intentionally match the established Workstation breadcrumb trail rather than introducing a new property-control scale.                                        | Consider tokenizing these values only as part of a broader Workstation-trail typography sweep. |
| `src/modules/ProjectManager/Projects/components/CreateProjectView/index.tsx:413` | Create-project organization trail | fix              | The former outlined pill used separate selector overrides. It now consumes the shared trail selector in fit-width mode while retaining its upward dropdown placement.                                                           | No change.                                                                                     |
| `src/engines/ChatPanel/panels/ProjectPanelView.tsx:557`                          | Floating properties pane          | fix              | The former full-height docked rail applied a different floating style. The complete inset, content-hugging panel now owns the environment trail's exact editor-canvas background, border, padding, radius, and dropdown shadow. | No change.                                                                                     |
| `src/engines/ChatPanel/panels/ProjectPanelView.tsx:213`                          | Properties visibility controls    | fix              | The shared 40px pane header now owns the persistent properties toggle, the panel title owns the `>>` collapse control, and the header joins the tab row without a second divider.                                               | No change.                                                                                     |
| `src/engines/ChatPanel/panels/ProjectPanelView.tsx:583`                          | Properties-pane row alignment     | fix              | Organization and the remaining property rows omit the legacy nested group inset in the trail variant, placing title text, row labels, and row icons on the Environment trail's shared 12px left axis.                           | Keep the legacy group inset enabled in non-trail property panels.                              |
| `src/engines/ChatPanel/panels/WorkItemPanelView.tsx:500`                         | Work Item properties trail        | fix              | Work Item details now use the same floating trail shell, title, toggle controls, flat property rows, 12px alignment axis, and content-hugging overflow behavior as Project details.                                             | Keep inline thread pills disabled while the trail pane owns these properties.                  |

## Summary

- Fix: 5
- Keep with reason: 1
- Abstract: 7
- Remaining cross-file sweep candidates: 0

The configured `frontend-ui-audit` skill file was unavailable in both the referenced user-global and workspace locations. This report follows the repository's documented audit table convention and covers the changed UI surfaces directly.
