# Tooltip content UI audit

| Line                                                   | Element                                | Verdict          | Reason                                                                                                                                                             | Suggested change                                                                                               |
| ------------------------------------------------------ | -------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| `src/components/Tooltip/index.scss:6`                  | Shared Tooltip viewport boundary       | fix              | The previous content-box maximum excluded padding and allowed max-content descendants to paint beyond the framed surface.                                          | Implemented `border-box`, viewport-aware maximum width, and shared descendant constraints.                     |
| `src/components/FilePathBreadcrumb/index.tsx:48`       | Full file-path breadcrumb              | fix              | `maxSegments={null}` is tooltip-only and previously forced every segment onto one unbounded flex line.                                                             | Implemented wrapping full paths with segment-level breaking while preserving the compact one-line row variant. |
| `src/components/KeyboardShortcut/index.tsx:300`        | Shared shortcut Tooltip content        | abstract         | This wrapper feeds 52 call sites, so resolving localized-label overflow here is one systematic sweep rather than site-by-site fixes.                               | Implemented a shrinkable label region; shortcut key caps remain non-shrinking.                                 |
| `src/components/ModelSelectionBreadcrumb/index.tsx:46` | Wide model breadcrumb                  | fix              | Wide mode duplicated Tooltip's 320/480px sizing and explicitly disabled wrapping, so long account/model identifiers could escape the panel.                        | Implemented parent-owned width and wrap-safe account/model labels; compact mode still truncates.               |
| `src/components/Tooltip/index.scss:10`                 | 320px and 480px Tooltip width variants | keep with reason | These are centralized geometry variants owned by the design-system Tooltip primitive, not repeated app-level arbitrary Tailwind values.                            | None.                                                                                                          |
| `src/components/Tooltip/index.tsx:515`                 | Tooltip's raw overlay DOM              | keep with reason | Tooltip is the design-system primitive itself and requires portal, measurement, content, and arrow elements that cannot be expressed by another Tooltip primitive. | None.                                                                                                          |
| `src/components/FilePathBreadcrumb/index.tsx:48`       | Presentational breadcrumb spans        | keep with reason | The breadcrumb is non-interactive text inside an already-owned Tooltip trigger; it introduces no click target, focus order, or missing keyboard action.            | None.                                                                                                          |

Verdict totals: **3 fix**, **3 keep with reason**, **1 abstract**.
