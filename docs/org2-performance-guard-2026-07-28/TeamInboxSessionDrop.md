# ORGII Performance Guard — Team Inbox Session Handoff

**Scope:** Session drop targeting, handoff preview/creation, recipient response, Team Inbox reconciliation, and collaboration projection.
**Date:** 2026-07-28
**Reviewer:** Codex

| Area               | Verdict | Evidence                                                                                                                                                                                                                                                                                                | Change or reason kept                                                                                                                                                                                                                    | Verification                                                                                                                                        |
| ------------------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Background work    | keep    | No timer, poller, worker, or persistent stream is introduced. `pointermove` exists only between eligible drag start and end/cancel; accept/return are request-driven.                                                                                                                                   | Listener and RAF cleanup run on end, cancel, disable, and unmount. Response commands use one blocking database task per explicit action.                                                                                                 | Drop lifecycle tests, Rust transition tests, and source lifecycle inspection.                                                                       |
| Memory             | keep    | Runtime state is one operation, one Session reference, one RAF point, and one single-flight entry per complete active intent (`viewer scope + session + project + recipient + title + note`). The alternative menu entry stores only the newest bounded Session reference until Team Inbox consumes it. | Preparation/creation entries delete themselves in `finally`; each consumer has caller-local cancellation without owning the shared request; notes and return reasons are capped; Session-derived description/todos/files remain bounded. | Mapper/form/shared-operation tests cover bounds, retry and independent cancellation; no app-lifetime handoff list or response cache was introduced. |
| Scope/isolation    | keep    | Creation keys include the complete mutation intent; recipient selection is revalidated from a freshly read active-project roster; response authorization is checked again in Rust.                                                                                                                      | Generation plus caller-local `AbortController` prevents stale UI completion without canceling a concurrent consumer; transition results update only the matching Work Item id/handoff id.                                                | Alias/self tests, unavailable-recipient path, shared cancellation test, unauthorized transition test, and full Rust application check.              |
| Rendering/hot path | keep    | Pointer coordinates remain in refs and hit testing is coalesced to one animation frame. Handoff presentation derives from the selected Work Item only.                                                                                                                                                  | No global Session subscription is attached to movement; response state is local and canonical refresh follows success.                                                                                                                   | 146 focused frontend tests, changed-surface ESLint, and TypeScript typecheck.                                                                       |
| Persistence/I/O    | keep    | Create writes Work Item plus handoff together. Reuse and accept/return use atomic Work Item partial transactions; return also changes assignment and clears obsolete receipts inside it.                                                                                                                | Collaboration outbox receives the same canonical handoff payload; roster reads are demand-driven and bounded to eight concurrent project reads; no follow-up repair loop or polling is added.                                            | 41 focused Rust FSM/projection/collaboration tests and `cargo check -p org2`.                                                                       |

- Performance verdict: pass
- Rendered two-account idle/request-count measurement was not required for a demand-driven path with no retained background resource; it remains part of manual collaboration QA.
