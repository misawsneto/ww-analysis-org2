# Frontend UI Audit — Session creator title and property placement

**Scope:** Work-item and human-session title placement plus work-item property placement in session creation.
**Summary:** 4 fix, 3 keep with reason, 0 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                                                                                                                                              Line | Element                      | Verdict          | Reason                                                                                                                                                                                               | Suggested change |
| ------------------------------------------------------------------------------------------------------------------------------------------------: | ---------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
|                                                                           `SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:277` | Interactive composer header  | fix              | Work-item and human-session title content previously rendered in a separate surface above the composer. It now uses the existing `EditorArea.headerContent` slot inside the shared `ComposerShell`.  | Completed.       |
| `ProjectManager/WorkItems/components/CreateWorkItemView/InlineCreateWorkItemFields.tsx:499` and `SessionCreator/variants/ChatPanel/index.tsx:702` | Title interaction state      | fix              | The two composer title inputs explicitly stay transparent on hover and focus, without changing the global ghost-input behavior.                                                                      | Completed.       |
|                                                                            `ProjectManager/WorkItems/components/CreateWorkItemView/index.tsx:270` | Title inset and separator    | fix              | Title-bearing composer shells use a reduced top inset, then end the title row with the established border token inside horizontal padding rather than an edge-to-edge rule.                          | Completed.       |
|                                                                           `SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:304` | Work-item property row       | fix              | The creation-only row omits the redundant “Add work item” action and selected project pill, keeping org/status/priority on the left, then a separator and the manage/pinned Skills & Tools controls. | Completed.       |
|                                                                                   `ChatPanel/InputArea/components/PinnedActionsBar/index.tsx:355` | Shared pinned-actions layout | keep with reason | A scoped `before-actions` placement reuses the established row, separator token, manage button, and scrollable action region without changing the two existing placements.                           | None.            |
|                                                                            `ProjectManager/WorkItems/components/CreateWorkItemView/index.tsx:410` | Manual work-item composer    | keep with reason | Manual mode has no external pinned-actions row, so it keeps the same property group in its internal tools row after a vertical separator while sharing the title treatment.                          | None.            |
|                                                                           `SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:224` | CLI/TUI title header         | keep with reason | CLI/TUI launch mode has no text composer, so its title context remains above the launch action; its property pills still use the shared external pinned-actions row.                                 | None.            |

No raw color, arbitrary color value, duplicated custom control, or global ghost-input behavior change was introduced. The systematic sweep covers human sessions, agent-assisted work-item creation, manual work-item creation, and CLI/TUI work-item launch; ordinary session launchers without title/property content are unchanged.
