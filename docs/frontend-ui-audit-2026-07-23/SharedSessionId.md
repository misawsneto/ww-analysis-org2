# Frontend UI Audit — Shared session ID

**Scope:** Canonical shared-session identity and copy affordances in the Team Sessions hovercard and overflow menu.
**Summary:** 2 fix, 4 keep with reason, 0 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                                                       Line | Element                         | Verdict          | Reason                                                                                                                                                                                                                       | Suggested change |
| ---------------------------------------------------------: | ------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
|           `SessionHoverCard/CloudSessionHoverCard.tsx:206` | Shared session ID row           | fix              | Team-session hovercards previously exposed descriptive metadata but no stable source identifier. The new row displays the canonical `sourceSessionId`, preserves the full value in its tooltip, and copies it on click.      | Completed.       |
| `WorkstationSidebarConnector/cloudSessionsSection.tsx:575` | Overflow copy action            | fix              | The Team Session `…` menu previously offered only Remove. It now leads with a localized “Copy ID” action and separates the destructive action below it.                                                                      | Completed.       |
|           `SessionHoverCard/CloudSessionHoverCard.tsx:206` | Hovercard row and fingerprint   | keep with reason | `HoverCardRow`, the 13px fingerprint icon, muted label, and middle-truncated identifier match the existing local-session ID row instead of introducing a separate visual pattern.                                            | None.            |
|           `SessionHoverCard/CloudSessionHoverCard.tsx:207` | Copy interaction and focus ring | keep with reason | A semantic button supplies a localized accessible name, keyboard focus treatment, and a transient success check; clipboard failure is logged without falsely showing success.                                                | None.            |
|            `SessionHoverCard/CloudSessionHoverCard.tsx:40` | Interactive text styling        | keep with reason | The class string intentionally matches the existing local-session and URL-row interactive text treatment. With only three small consumers, a shared component would add API surface without reducing behavioral duplication. | None.            |
| `WorkstationSidebarConnector/cloudSessionsSection.tsx:576` | Native menu and feedback        | keep with reason | The action reuses the existing native menu, shared clipboard fallback, localized success/error messages, and a standard separator rather than adding a custom dropdown surface.                                              | None.            |

No new raw color, arbitrary Tailwind value, inaccessible click target, or custom menu control was introduced. The full identifier remains available to pointer users through the native title tooltip and to all users through the copy action. The systematic sweep found the existing local-session ID row already covers local sidebar sessions; the reference action belongs specifically to cloud teammate rows because it requires the org + owner + source-session tuple.
