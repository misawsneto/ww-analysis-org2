# Frontend UI Audit — Turn metadata footer

**Scope:** Turn-level edit/read tabs, resource rows, expanded scrolling, and the expand/collapse control.
**Summary:** 3 fix, 4 keep with reason, 0 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                               Line | Element                                                | Verdict          | Reason                                                                                                                                                                          | Suggested change                                                                                                      |
| ---------------------------------: | ------------------------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `TurnMetadataFooter/index.tsx:108` | Edit/read tab typography                               | fix              | The mini tabs and hard-coded 10px counts made the turn summary materially smaller than adjacent chat content, while tab icons added unnecessary visual noise.                   | Completed: use the chat-panel tab size and shared `chat-block-xs` count token with text-only, baseline-aligned pills. |
| `TurnMetadataFooter/index.tsx:254` | Review, artifact, and resource rows                    | fix              | Several labels were fixed at 12px/11px and read/file icons were 14px, bypassing the user-adjustable chat typography hierarchy.                                                  | Completed: use `chat-block-title`/`chat-block-xs` and 16px primary icons.                                             |
| `TurnMetadataFooter/index.tsx:264` | Expanded scroll area and collapse control              | fix              | The collapse action participated in the scrolling list and the native scrollbar remained visible, so the action could be pushed offscreen.                                      | Completed: isolate a scrollbar-hidden flex scroll area and keep the control in a non-scrolling footer.                |
|             `FileChangeRow.tsx:24` | Optional file icon sizing                              | keep with reason | The shared row remains the correct design-system component. An optional size prop lets this larger turn surface request 16px without changing compact composer consumers.       | None. The existing 14px default is preserved.                                                                         |
| `TurnMetadataFooter/index.tsx:244` | `TabPill`, `TextButton`, and `StackRowButton` controls | keep with reason | These components already provide the established active, hover, keyboard-focus, disabled, and button semantics.                                                                 | None.                                                                                                                 |
| `TurnMetadataFooter/index.tsx:265` | 320px expanded-height cap                              | keep with reason | This pre-existing cap prevents a large turn summary from dominating the conversation viewport and is still needed after the control is pinned.                                  | None.                                                                                                                 |
| `TurnMetadataFooter/index.tsx:348` | Expand/collapse accessibility                          | keep with reason | The control remains a native button through `StackRowButton`, retains a visible label, exposes `aria-expanded`, and is keyboard reachable after moving outside the scroll area. | None.                                                                                                                 |

No raw colors, new icon implementation, or duplicate custom control was introduced. The text-only pill treatment and size change are deliberately scoped to the turn metadata footer, so there is no multi-file sweep candidate.
