# Built-in Tools dev-mode visibility — frontend UI audit

Scope: the Settings sidebar, Settings breadcrumb selector, Global Spotlight destination, direct Settings routes, and the Built-in Tools/Computer Use integration content boundary. The routed `frontend-ui-audit` skill file was unavailable at both repository-documented locations, so this report follows the fallback table convention in `AGENTS.md` and the existing audit-batch precedent.

| Line                                                                       | Element                                     | Verdict          | Reason                                                                                                                             | Suggested change                                                                                             |
| -------------------------------------------------------------------------- | ------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `SettingsSidebar.tsx:180,198-204,237-267`                                  | Settings sidebar Built-in Tools row         | fix              | The row was always derived from the static Tools group, so non-dev users could discover and open the internal surface.             | Filter mixed Settings item lists through the shared dev-mode availability rule.                              |
| `SettingsBreadcrumb/index.tsx:159-188`                                     | Settings breadcrumb selector                | fix              | The header selector duplicated the Settings navigation registry and would otherwise keep exposing the hidden destination.          | Apply the same dev-mode item filter before building selector entries.                                        |
| `navDestinationGroups.ts:89-103`, `spotlightSearchBuilder.ts:31-100`       | Global Spotlight Built-in Tools destination | fix              | Spotlight provided a third navigation path that bypassed sidebar visibility.                                                       | Mark the destination `devOnly` and exclude it from search/default navigation while Dev Mode is off.          |
| `SettingsSlot.tsx:273-281,390-403`                                         | Direct `/settings/integrations/tools` route | fix              | Hiding links alone still allowed direct URLs and persisted navigation to mount the Integrations body.                              | Redirect blocked routes to Models before the Integrations body mounts.                                       |
| `IntegrationsDetailPanel.tsx:49-53,185-201`, `ToolsCategoryView.tsx:1-102` | Built-in Tools component/loading boundary   | fix              | The tools metadata, agent configs, matrix, table, and playground were imported and mounted as part of ordinary Integrations usage. | Lazy-load the dev-only component and move its hooks/toolbar ownership inside that route-local component.     |
| `ComputerUseCategoryView.tsx:1-47`                                         | Computer Use settings surface               | keep with reason | Computer Use remains a normal Settings destination and does not require the Built-in Tools metadata/table hooks.                   | Keep it in a separate lazy component so opening Computer Use does not load the dev-only table surface.       |
| `mainAppPaths/integrations.ts:35-60,159-167`                               | Dev-only integration policy                 | abstract         | Visibility and access checks span multiple navigation surfaces and need one authoritative rule.                                    | Centralize availability, list filtering, and direct-route redirect decisions in the integration path config. |

Verdict counts: **fix 5**, **keep with reason 1**, **abstract 1**.

Accessibility check: hidden destinations are removed from rendered keyboard-navigation collections rather than visually concealed. Dev-mode users retain the same native button/menu semantics and route behavior. Blocked direct links replace history and land on the visible Models destination.

Design-system check: no visual tokens, arbitrary styles, or interaction primitives changed. Existing shared NavigationMenu, breadcrumb dropdown, Suspense placeholder, and toolbar atoms remain in use.

Systematic sweep: Settings sidebar, breadcrumb selector, Spotlight, direct route entry, Integrations toolbar ownership, Built-in Tools data hooks, and the adjacent Computer Use surface were checked together. No non-dev navigation path to Built-in Tools remains.
