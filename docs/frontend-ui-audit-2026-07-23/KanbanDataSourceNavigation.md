# Frontend UI Audit — Kanban Data Source Navigation

**Scope:** Removal of the duplicate Kanban data-source surface and replacement with navigation to Runtime.
**Summary:** 5 fix, 1 keep with reason, 1 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                              Line | Element                              | Verdict          | Reason                                                                                                                                             | Suggested change                                                                                                                           |
| --------------------------------: | ------------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
|    `FactoryViewPill/index.tsx:20` | Kanban view-mode type                | fix              | Data Sources is no longer a renderable Kanban view, so retaining it in the union would preserve a false local ownership path.                      | Completed: restricted the view union and URL parser to Kanban, List, and Diary; stale `?view=datasource` URLs resolve to Kanban.           |
|    `FactoryViewPill/index.tsx:48` | Data Sources header affordance       | fix              | The label remains useful for discovery, but it should signal that activation leaves the local view family.                                         | Completed: retained the localized label and added a decorative trailing arrow-up-right icon that appears on hover.                         |
|    `FactoryViewPill/index.tsx:65` | Runtime navigation                   | keep with reason | The existing Runtime opener is the canonical singleton path and already focuses an open Runtime tab, preventing a second data-source destination.  | Completed: call `openRuntimeInChatPanelTabAtom` and leave the Kanban search parameters untouched.                                          |
|       `TabPill/tabContent.tsx:48` | Trailing hover adornment             | abstract         | Reserving the icon width avoids tab jitter, and the behavior belongs in the shared tab primitive rather than a Kanban-specific selector or style.  | Completed: added `hoverBadge` to `TabPillItem`; it occupies stable trailing space and becomes visible through the primitive's hover state. |
|  `TaskKanbanContent/index.tsx:13` | Duplicate Data Source render branch  | fix              | Lazy loading reduced initial cost but still created a second live UI and ownership path for content now canonically composed by Runtime.           | Completed: removed the data-source import and switch branch; only Diary and List remain as secondary Kanban chunks.                        |
|      `useTaskKanbanHeader.tsx:61` | Dead Data Source header special case | fix              | The local branch existed only to suppress Kanban controls while the duplicate panel was active; retaining it would imply an unreachable view mode. | Completed: removed the special case so header behavior is exhaustive over the reduced `FactoryViewMode`.                                   |
| `shared/dataSource/index.tsx:115` | Retired multi-host panel API         | fix              | Controlled view, hidden header, optional Runtime sections, and scrollbar variants had no production caller after Kanban stopped hosting the panel. | Completed: made Runtime sections explicit and removed the obsolete props, conditional branches, public exports, and tests.                 |

No new raw color, arbitrary Tailwind value, bespoke button, or duplicate content surface was introduced. The arrow-up-right SVG is decorative while the existing localized button label remains the accessible name. The systematic sweep found no remaining production `datasource` render branch or Task Kanban import of the shared data-source panel.
