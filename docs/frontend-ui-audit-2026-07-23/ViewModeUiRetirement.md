# View-mode UI retirement audit

Scope: UI-shell cleanup required to remove Home, the old App Grid, detached-window pages, and global view modes. The documented `frontend-ui-audit` skill was unavailable in both configured locations, so this report applies the repository's audit convention directly.

| Line                                                        | Element                    | Verdict          | Reason                                                                                                                                                           | Suggested change                                                           |
| ----------------------------------------------------------- | -------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `src/app/root/services/GlobalDragDrop/index.tsx:12`         | Global drag state bridge   | fix              | The Home-only repository hint and ad-hoc confirmation modal were removed; the remaining component only exposes the tokenized ChatPanel composer highlight state. | Landed: shared drag/drop no longer renders a retired Home surface.         |
| `.archive/src/modules/MainApp/StartPage/index.tsx:1`        | Old Home App Grid          | fix              | The route and all live entry points were removed, so keeping its page/card UI in the bundle would be dead presentation code.                                     | Landed: archived with its local components, hooks, styles, and tests.      |
| `.archive/src/windows/ModeSelectionWindow.tsx:1`            | Detached-window pages      | fix              | The welcome picker and detached tab host had no production caller after global view-mode retirement.                                                             | Landed: archived the pages and window-only styles as one restoration unit. |
| `.archive/src/modules/MainApp/Settings/index.tsx:1`         | Full-page Settings shell   | fix              | This shell was reachable only through the retired detached tab host and duplicated the active Workbench slot.                                                    | Landed: archived the shell and child-only route/toolbar/list logic.        |
| `src/modules/MainApp/Settings/SettingsSlot.tsx:77`          | Workbench Settings slot    | keep with reason | It is the active route-owned Settings surface and reuses the existing renderers, sections, integrations, headers, and resize system.                             | None.                                                                      |
| `src/modules/shared/layouts/sidebar/SidebarSelector.tsx:16` | Workbench sidebar selector | keep with reason | It mounts exactly one established sidebar variant from route ownership and retains guide/tour targets without duplicating sidebar markup.                        | None.                                                                      |
| `src/modules/shared/layouts/sidebar/FloatingSidebar.tsx:14` | Collapsed sidebar surface  | keep with reason | It reuses the same Workstation and Settings sidebar components as the docked selector, preserving visual and interaction parity.                                 | None.                                                                      |
| `src/modules/shared/layouts/MainAppShell/index.tsx:22`      | Standalone page shell      | keep with reason | The shell retains existing background/header tokens and overflow structure while using the router outlet directly.                                               | None.                                                                      |
| `src/modules/AppLogin/index.tsx:320`                        | Login onboarding layout    | keep with reason | The established shared onboarding layout remains unchanged; obsolete global toolbar-hiding CSS/body state is no longer needed.                                   | None.                                                                      |
| `src/modules/MainApp/SelectRepo/index.tsx:470`              | Repository picker layout   | keep with reason | The established onboarding and picker components remain; the retired global Home/view-mode shell is not required.                                                | None.                                                                      |

Verdict totals: **4 fix**, **6 keep with reason**, **0 abstract**.

Systematic sweep: no Home sidebar, old App Grid, repository-drop overlay, detached-window UI, full-page Settings duplicate, or hidden inactive sidebar mount remains in live UI source. Workstation, Workbench Settings, Login, Select Repo, ChatPanel Launchpad, and domain-local view toggles retain their established design-system components.
