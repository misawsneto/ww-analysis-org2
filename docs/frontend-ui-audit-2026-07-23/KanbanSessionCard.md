# Frontend UI Audit — Kanban session identity

**Scope:** Session-card title/metadata hierarchy and session identity parity across Kanban, hover cards, and Sidebar.
**Summary:** 4 fix, 5 keep with reason, 0 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                                                   Line | Element                       | Verdict          | Reason                                                                                                                                                                                              | Suggested change |
| -----------------------------------------------------: | ----------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
|        `KanbanBoard/components/TaskCard/index.tsx:109` | Session app identity          | fix              | The app icon and app label previously occupied scarce footer space even though the icon is the useful scanning cue. The icon now leads the title and retains the app name as its accessible label.  | Completed.       |
|   `util/session/sessionDisplayMetadata.ts` and callers | Shared session identity       | fix              | Cloud/local hover, cloud/local/imported Kanban, and cloud/local Sidebar previously interpreted identity independently, allowing the same session to show different brands by surface.               | Completed.       |
|  `TaskKanban/components/KanbanHeaderFilters/index.tsx` | Kanban agent filters          | fix              | Header options and active filtering read raw local session fields, so cloud/imported rows could render correctly but fail their visible agent filter. They now consume the projected task identity. | Completed.       |
| `WorkstationSidebarConnector/cloudSessionsSection.tsx` | Cloud Sidebar identity        | fix              | Cloud rows retained a separate external/CLI/native icon branch. They now use the same resolver result as cloud Kanban and hover; fork lineage remains a deliberate row-level glyph.                 | Completed.       |
|        `KanbanBoard/components/TaskCard/index.tsx:156` | Session model metadata        | keep with reason | The existing formatted model label is reused as plain text and now leads the compact impact row; removing its duplicate provider icon matches the requested hierarchy.                              | None.            |
|   `KanbanBoard/components/TaskImpactLine/index.tsx:31` | Change and file counts        | keep with reason | The existing shared impact component already supplies localized number formatting, diff colors, separators, and the file-change icon. Moving it preserves those established semantics.              | None.            |
|        `KanbanBoard/components/TaskCard/index.tsx:151` | Non-session card layout       | keep with reason | The compact branch is keyed by session agent metadata, so work-item and todo cards retain their established impact and auxiliary-metadata rows.                                                     | None.            |
|       `SessionHoverCard/CloudSessionHoverCard.tsx:109` | Cloud agent/model row         | keep with reason | The established hover-card row structure, provider icon, and model icon remain; only their label/icon/model inputs now come from the shared resolver.                                               | None.            |
|     `SessionHoverCard/SessionHoverCardContent.tsx:309` | Imported replay model display | keep with reason | Imported replays show the source model for read-only context while the fork composer still receives no runnable model selection.                                                                    | None.            |

No new raw color, arbitrary Tailwind value, duplicate icon implementation, or custom interactive control was introduced. The app icon uses the existing agent-icon resolver and has an accessible label; title truncation and retry-badge behavior remain intact. The systematic sweep routes Kanban Board/List/Diary, Kanban filters, hover cards, and local/cloud Sidebar rows through one identity pipeline. Agent Team and benchmark precedence moved into that resolver; Sidebar keeps only a component adapter and the cloud fork-lineage glyph.
