# GitHub work-item header — frontend UI audit

Scope: GitHub Issues/PR published-header controls, their dedicated search row, and the shared repository filter. The routed `frontend-ui-audit` skill file was unavailable, so this report follows the repository's documented audit format and existing GitHub-header audit precedent.

| Line                                                                               | Element                            | Verdict          | Reason                                                                                                                                                                                                                                         | Suggested change                                                                   |
| ---------------------------------------------------------------------------------- | ---------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `GitHubWorkItemsView.tsx:171-220,260-265,432-447`                                  | Published controls and search rows | fix              | Issues and PRs now share the requested control order: repository, Open/Closed state, optional personal filter, then search. Search stays on that row when space permits and moves to a full-width second row below the established breakpoint. | Keep the control order stable across both responsive layouts.                      |
| `GitHubWorkItemControls.tsx:83-132`                                                | Repository filter select           | fix              | This is a compact header filter and should share the transparent ghost-select treatment used by Kanban header filters, while preserving its searchable menu and compact repository label.                                                      | Use the shared `Select` with `size="small"`, `variant="ghost"`, and `radius="lg"`. |
| `WorkManagement/index.tsx`, `GitHubWorkItemsSurface.tsx`, `GitHubWorkItemList.tsx` | Responsive single-row plumbing     | keep with reason | The existing element-width measurement and 650px threshold make the row decision from the actual host surface rather than viewport assumptions. The same boolean controls search placement and the joined-row divider.                         | Keep the breakpoint centralized and covered by a boundary test.                    |
| `GitHubWorkItemList.test.ts`, `GitHubWorkItemControls.test.ts`                     | Header regression coverage         | fix              | Focused tests cover the 649/650px boundary, flexible search sizing in either row, and the repository trigger's shared ghost variant.                                                                                                           | Keep the breakpoint and ghost-class assertions.                                    |
| `GitHubWorkItemList.tsx:91-145`                                                    | Open/Closed state tabs             | keep with reason | Open/Closed is a mutually exclusive navigation state, so the shared `TabPill` remains clearer than converting it to a data select. It already uses the compact 28px design-system treatment.                                                   | None.                                                                              |
| `GitHubWorkItemControls.tsx:134-178`                                               | Personal issue filter action       | keep with reason | This is a multi-select filter action with an active-count accessible label, not a single-value select. Its compact icon button distinguishes the optional filter menu from the repository selector.                                            | Keep the existing shared Dropdown + Button composition.                            |
| `GitHubWorkItemControls.tsx:476-494`                                               | Create-issue repository select     | keep with reason | The select is a labeled form field inside a modal, where the bordered default affordance communicates input boundaries better than a toolbar-style ghost control.                                                                              | Keep the default form-select treatment.                                            |

Verdict counts: **fix 3**, **keep with reason 4**, **abstract 0**.

Accessibility check: the dedicated search input keeps its explicit aria label; Open/Closed controls remain native labeled buttons; the repository filter remains keyboard-operable through the shared Select; the personal-filter button retains its selected-count aria label.

Design-system check: no new spacing, color, radius, dropdown, or responsive primitive was introduced. The change removes a one-off breakpoint and adopts the existing ghost Select variant already used by Kanban header controls.

Systematic sweep: `singleRowHeader` is propagated only through the Work Management GitHub surface and controls only search placement plus joined-row chrome. Form selects were intentionally excluded from the ghost-toolbar sweep.
