# Frontend UI Audit — Launchpad Navigation

**Scope:** Launchpad inner navigation, creation-target discovery, embedded Work Item composer, and utility action grouping.

The repository-referenced `frontend-ui-audit` skill was unavailable at both documented paths, so this report follows the fallback table convention in `AGENTS.md`.

| Line                                                                                 | Element                      | Verdict          | Reason                                                                                                                                                                                                      | Suggested change                                                                                              |
| ------------------------------------------------------------------------------------ | ---------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:240`                                   | Launchpad navigation row     | keep with reason | The row reuses the shared `TabPill` simple variant, Runtime's centered content width, and the chat-pane surface token instead of adding a local tab primitive.                                              | None.                                                                                                         |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:243`                                   | Session / Work Item / More   | fix              | All three views now remain inside the Launchpad page, while the compact utility pills stay reachable beneath the primary Session and Work Item launchers.                                                   | Default to Session, embed the full work-item creator, and retain update/import/API-key actions below.         |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:272`                                   | Conditional view content     | keep with reason | One active view is rendered at a time, which keeps keyboard focus and screen-reader navigation from reaching controls hidden by another tab.                                                                | None.                                                                                                         |
| `src/engines/ChatPanel/ChatPanelHeader.tsx:270`                                      | Legacy surface header UI     | fix              | Creator selection, titles, breadcrumbs, back navigation, presence, and Agent mode now live on their respective panes; keeping parallel header branches duplicated state and left unreachable fallback code. | Limit the chat-pane header to the real tab strip, session toolbar, and the work-item trailing action.         |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:315`                                   | More creation selector       | keep with reason | More reuses the translated creation-target options and compact Select styling for Agent / Skills management, project creation, and the combined add/join organization flow.                                 | Keep the remaining selection and creation content inside More without promoting modes to separate states.     |
| `src/store/chatPanel/chatPanelTabOpenAtoms.ts:79`                                    | Creator target routing       | fix              | Creator buttons previously opened legacy standalone surfaces even though Work Item, Project, GitHub Issues, and organization creation now belong to Launchpad's pinned views.                               | Focus the singleton Launchpad tab and set its embedded target and organization context in one state action.   |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:240`                                   | Creator cancellation         | fix              | Form cancellation must return to the pinned Session creator rather than leaving Launchpad for a bare session surface.                                                                                       | Synchronize the pinned view when the creation target returns to Agent Session.                                |
| `src/engines/ChatPanel/ChatPanelEmptyContent.tsx:160`                                | Work Item composition        | keep with reason | Launchpad and direct work-item entry reuse the same creator composition; Agent mode receives Session Creator while manual mode remains a direct Work Item editor inside the same visual frame.              | Keep the Agent toggle inline when the creator is hosted inside Launchpad.                                     |
| `src/modules/ProjectManager/WorkItems/components/CreateWorkItemView/index.tsx:355`   | Unified Work Item composer   | fix              | The manual path previously rendered its project editor and Save action as a separate form and footer even though the editor already supported `@` references and skill pills.                               | Keep manual Work Item logic, preserve the rounded composer shell, and reuse the shared `+` and Save controls. |
| `src/modules/ProjectManager/WorkItems/components/WorkItemContentStack.tsx:64`        | Optional content dividers    | fix              | The attached title and metadata row inherited horizontal and vertical separators that visually split one compact composer header.                                                                           | Disable dividers for the attached composer row while retaining them by default elsewhere.                     |
| `src/modules/ProjectManager/shared/components/ProjectContentEditor/index.tsx:28`     | Manual editor action seam    | keep with reason | The existing project editor owns manual description state, `@` context, slash skills, image handling, and serialization; narrow ref methods let the shared toolbar open those existing menus.               | None.                                                                                                         |
| `src/features/SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:169` | Attached composer header row | keep with reason | The new top row mirrors the existing session-info row below the input, reusing the same composite-card layering while leaving the message input shell and its focus treatment independent.                  | None.                                                                                                         |
| `src/engines/ChatPanel/ChatPanelStartPage.test.ts:16`                                | View-state coverage          | fix              | The former tests assumed every action was visible at once and explicitly asserted that no inner tabs existed.                                                                                               | Cover Session, Work Item, and More independently, including creation targets and the conditional update.      |
| `src/i18n/locales/en/sessions.json:926`                                              | Navigation labels            | fix              | User-facing tab labels must remain localized consistently across all supported locales.                                                                                                                     | Add Session, Work Item, and More keys to each sessions locale.                                                |

## Verdict summary

- Fix: 8
- Keep with reason: 6
- Abstract: 0
- Multi-file sweep candidates: 0
