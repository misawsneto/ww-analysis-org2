# Frontend UI Audit — Runtime Navigation

**Scope:** Runtime sidebar placement, shared chat-header integration, Runtime inner navigation, Launchpad cleanup, and Usage/Assets content placement.

The repository-referenced `frontend-ui-audit` skill was unavailable at both documented paths, so this report follows the fallback table convention in `AGENTS.md`.

| Line                                                                           | Element                      | Verdict          | Reason                                                                                                                                                                                                                                     | Suggested change                                                                                   |
| ------------------------------------------------------------------------------ | ---------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- |
| `src/scaffold/NavigationSidebar/connectors/workstationSidebarMenuItems.tsx:82` | Runtime sidebar item         | fix              | Runtime needs a stable top-level destination directly below Work Items, not a Launchpad subview. The item reuses the existing navigation-item model and Lucide icon system.                                                                | Add the Runtime item after Work Items and route it to the singleton chat tab.                      |
| `src/engines/ChatPanel/index.tsx:230`                                          | Runtime chat-header behavior | fix              | Runtime is a first-class chat surface, so it must keep the normal header available for other tab pills, the plus menu, and the My Station maximize/restore control.                                                                        | Treat Runtime as a standalone tool surface while still forcing the shared header visible.          |
| `src/modules/shared/dataSource/index.tsx:608`                                  | Runtime inner navigation row | keep with reason | Usage / Scanning / Hooks / Assets describe sections inside Runtime, not peer chat tabs. Keeping the established `TabPill` below the chat header preserves the correct information hierarchy and matches Settings-style content navigation. | None.                                                                                              |
| `src/modules/shared/dataSource/index.tsx:617`                                  | Four-section order           | fix              | Usage is the primary Runtime overview and should open first; Assets replaces the old Manage dashboard and remains last. Kanban retains its existing sections without Assets.                                                               | Order the sections Usage / Scanning / Hooks / Assets and default Runtime to Usage.                 |
| `src/modules/shared/dataSource/index.tsx:783`                                  | Usage content                | fix              | Quota is usage information and was misplaced in the old Manage/dashboard surface.                                                                                                                                                          | Render the quota grid above the existing usage dashboard.                                          |
| `src/engines/ChatPanel/panels/RuntimePanelView.tsx:8`                          | Runtime composition          | keep with reason | A small composition component makes the Usage/Assets ownership obvious without coupling the shared data-source module to ChatPanel-specific dashboard components.                                                                          | None.                                                                                              |
| `src/engines/ChatPanel/ChatPanelStartPage.tsx:182`                             | Launchpad Work surface       | fix              | Work, Manage, and Runtime no longer share one local tab row; leaving that selector would duplicate the new sidebar/chat hierarchy.                                                                                                         | Remove the inner selector and render only the Work launcher.                                       |
| `src/modules/shared/dataSource/index.tsx:614`                                  | Width and spacing tokens     | keep with reason | The Runtime row reuses the panel's existing `max-w-[932px]`, spacing, background, and `TabPill` patterns; this change does not introduce a new visual primitive.                                                                           | Consider replacing the pre-existing arbitrary max width only in a separate panel-wide token sweep. |

## Verdict summary

- Fix: 5
- Keep with reason: 3
- Abstract: 0
- Multi-file sweep candidates: 0
