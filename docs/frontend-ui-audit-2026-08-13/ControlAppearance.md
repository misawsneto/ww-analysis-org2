# Control appearance consolidation

## Scope

Audited shared inputs, selects, date/time controls, selector pills, grouped
pills, tab pills, settings-table filters, and status-bar buttons for duplicate
or overloaded ghost/tertiary presentation APIs.

## Findings

| Line                                                                                                                | Element                           | Verdict          | Reason                                                                                                                                                                           | Suggested change                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------------------------------------------------------------------- |
| `src/components/controlAppearance.ts:8`                                                                             | Shared appearance vocabulary      | abstract         | Input, Textarea, Select, and visual wrapper controls independently described the same transparent idle treatment with `fieldVariant`, `variant`, `colorScheme`, or paired flags. | Own `default`, `ghost`, and `bare` names in one shared type vocabulary.                                                                       |
| `src/components/Input/index.tsx:128`                                                                                | Input appearance                  | fix              | `fieldVariant`, `borderless`, and `bgless` could express overlapping or contradictory states.                                                                                    | Replace them with one `appearance` prop; use `ghost` for interactive surface feedback and `bare` for permanently chromeless editor internals. |
| `src/components/Textarea/index.tsx:107`                                                                             | Textarea appearance               | fix              | Textarea duplicated Input's three-prop state model.                                                                                                                              | Use the same `FieldAppearance` contract and state behavior as Input.                                                                          |
| `src/components/Select/types.ts:75`                                                                                 | Select appearance                 | fix              | `variant="ghost"` described visual treatment while `variant` elsewhere represents semantic importance or structural layout.                                                      | Rename to `appearance` and support the same field vocabulary.                                                                                 |
| `src/components/DatePicker/index.tsx:87`, `src/components/TimePicker/index.tsx:12`                                  | Date/time ghost states            | fix              | Focus used `fill-2` while the shared ghost fields used `surface-hover`.                                                                                                          | Use `appearance` and the same hover/focus surface token.                                                                                      |
| `src/components/SelectorPill/index.tsx:202`                                                                         | Selector pill visual treatment    | fix              | The old ghost name actually disabled all hover/open painting, unlike ghost fields.                                                                                               | Name the fully chromeless treatment `bare`; keep default pills on shared hover/open surfaces.                                                 |
| `src/components/PillGroup/index.tsx:239`                                                                            | Pill group strong surface         | abstract         | `appearance="ghost"` and `strongSurface` selected the same fill-3 state, and the production caller supplied both.                                                                | Remove the duplicate appearance axis and keep the explicit contrast control.                                                                  |
| `src/components/TabPill/types.ts:16`                                                                                | Tab pill appearance               | fix              | `colorScheme` mixed structural surface choices with the same ghost presentation used by fields.                                                                                  | Rename to `appearance` and align ghost active/open state with `surface-hover`.                                                                |
| `src/components/SettingsTable/index.tsx:112`                                                                        | Settings-table select descriptors | fix              | The wrapper leaked Select's old visual `variant` name through its public descriptor API.                                                                                         | Forward `appearance` directly and keep ghost as the compact-toolbar default.                                                                  |
| `src/modules/WorkStation/shared/StatusBar/StatusBarBase.tsx:49`                                                     | Status bar semantic kind          | fix              | `variant="ghost"                                                                                                                                                                 | "primary"` mixed visual treatment with semantic importance.                                                                                   | Use semantic `primary                 | tertiary`; retain ghost as the status bar's fixed visual treatment. |
| `src/modules/ProjectManager/shared/components/PropertiesPanel/PropertyFieldSections/PeopleTeamsLabelsFields.tsx:46` | Property `fieldVariant="row"      | "pill"`          | keep with reason                                                                                                                                                                 | This value changes structural layout, dropdown width, alignment, and label placement rather than ghost field appearance.                      | Keep the domain-specific layout name. |
| `src/components/PropertyField/PropertyDropdownField.tsx:65`                                                         | Property-field `borderless`       | keep with reason | Border presence is an independent option for property pills whose background and focus treatment remain intact.                                                                  | Keep the explicit border control; do not map it to a chromeless field appearance.                                                             |
| `src/modules/MainApp/ToolPreview/mockData/events/fileOps.ts:17`                                                     | Mock source-code string           | keep with reason | This is literal demo payload representing an external example, not a live ORGII component API.                                                                                   | Preserve fixture content.                                                                                                                     |

## Summary

- Fix: 8
- Keep with reason: 3
- Abstract: 2
- Remaining cross-file sweep candidates: 0

The configured `frontend-ui-audit` skill file was unavailable in both the
referenced user-global and workspace locations. This report follows the
repository's documented audit table convention and records the full usage
sweep performed for this refactor.
