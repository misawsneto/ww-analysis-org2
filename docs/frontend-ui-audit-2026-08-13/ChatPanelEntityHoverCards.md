# Frontend UI Audit — Chat Panel Entity Hover Cards

Scope: chat-pane session, work-item, and pull-request tab triggers plus the existing sidebar hover-card components they reuse.

| Line                                                                              | Element                                             | Verdict          | Reason                                                                                                                                                                                                                                                                                   | Suggested change                                                                                                                               |
| --------------------------------------------------------------------------------- | --------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/engines/ChatPanel/ChatPanelTabBar.tsx:155`                                   | Work-item tab metadata adapter                      | fix              | Chat tabs store the canonical UI work-item shape, while the sidebar preview consumes a compact display shape. The adapter now translates at the trigger boundary instead of duplicating card markup.                                                                                     | Keep tab-to-preview mapping beside the centralized tab wrapper.                                                                                |
| `src/engines/ChatPanel/ChatPanelTabBar.tsx:174`                                   | Pull-request tab metadata adapter                   | fix              | The existing PR card uses API-style field names; the adapter preserves that card and its status rules without adding a second PR preview implementation.                                                                                                                                 | Keep the optional updated timestamp on the tab detail payload so newly opened PR tabs show the same useful recency row.                        |
| `src/engines/ChatPanel/ChatPanelTabBar.tsx:190`                                   | Tab hover-card selection                            | abstract         | Three separate trigger branches would invite drift as tab types evolve. `TabPillHoverCard` owns the entity-to-card selection and keeps every card below the horizontal tab strip with the same position.                                                                                 | Add future entity preview types to this wrapper instead of branching at the end of `TabPill`.                                                  |
| `src/modules/ProjectManager/WorkItems/components/WorkItemHoverCard/index.tsx:33`  | Work-item hover-card public data contract           | fix              | The reusable card previously required full sidebar records even though it renders only identity, state, ownership, labels, and timestamps. The compact structural contract lets sidebar rows and chat tabs share the same component.                                                     | Keep source-specific fetching and record enrichment outside the visual component.                                                              |
| `src/modules/ProjectManager/WorkItems/components/WorkItemHoverCard/index.tsx:125` | Optional project and organization rows              | fix              | Chat-pane work items can legitimately lack one of these labels. Conditional rows avoid blank icon/value lines while retaining both rows for sidebar data.                                                                                                                                | No change.                                                                                                                                     |
| `src/components/PrHoverCard/index.tsx:18`                                         | PR hover-card public data contract                  | fix              | The card previously required the complete GitHub list record despite rendering only title, state, number, URL, branches, update time, and diff stats. Narrowing the contract removes API-record coupling while allowing both sidebar and chat-tab previews to receive the same metadata. | Keep additions and deletions optional so older persisted tabs and failed metadata enrichment omit the row instead of showing misleading zeros. |
| `src/components/PrHoverCard/index.tsx:91`                                         | PR change-scope row                                 | keep with reason | The preview reuses the shared `HoverCardRow`, `FileDiff` icon, and `DiffStatsBadge` green/red number treatment, matching existing diff surfaces without introducing card-specific colors or typography.                                                                                  | Keep future diff-stat styling changes inside `DiffStatsBadge`; do not recreate `+`/`-` spans in the hover card.                                |
| `src/components/SessionHoverCard/HoverCardBase.tsx:299`                           | Shared hover trigger, singleton, portal, and delays | keep with reason | Session, work-item, and PR previews all continue to use the same trigger cloning, delayed open/close behavior, singleton ownership, portal placement, and click dismissal.                                                                                                               | Do not add tab-specific hover timers or portals.                                                                                               |
| `src/engines/ChatPanel/ChatPanelTabBar.tsx:480`                                   | Existing tab pill and close controls                | keep with reason | Hover cards wrap the established tab element; role, selection state, activation, middle-click close, context menu, drag behavior, close button, and focus styles remain owned by the existing tab primitives.                                                                            | No change.                                                                                                                                     |

## Summary

- Fix: 5
- Keep with reason: 3
- Abstract: 1
- Remaining cross-file sweep candidates: 0

No arbitrary Tailwind values, new card surfaces, or new interactive elements were introduced. The configured `frontend-ui-audit` skill file was unavailable at both documented locations, so this report follows the repository's audit-table convention and covers the changed UI surfaces directly.
