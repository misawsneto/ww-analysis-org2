# Discussion Channels — Frontend UI Audit

**Date:** 2026-07-30

**Scope:** channel dialogs and message rows changed by PR #610 plus deferred follow-ups

**Method note:** the routed `frontend-ui-audit` skill was not present in either configured skill location, so this report applies the repository-required output columns and design-system checks directly.

| Line                                                                                | Element                                             | Verdict          | Reason                                                                                                                                                                                | Suggested change                                                                                                  |
| ----------------------------------------------------------------------------------- | --------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `DiscussionChannels/components/ChannelDialogPrimitives.tsx:142`                     | Eight create/settings/archive/delete dialog footers | abstract         | The controls had identical alignment, secondary cancel, primary/danger submit, loading, disabled, and test-id behavior. A shared primitive removes drift without owning dialog state. | Landed as `ChannelDialogFooter`.                                                                                  |
| `DiscussionChannels/components/ChannelDialogPrimitives.tsx:112`                     | Two destructive confirmation bodies                 | abstract         | Cloud and local delete used the same danger notice plus explicit acknowledgement control.                                                                                             | Landed as `ChannelDeleteConfirmation`.                                                                            |
| `DiscussionChannels/components/ChannelDialogPrimitives.tsx:25`                      | Name/topic fields and error notice                  | keep with reason | These already use design-system `Input`, associated labels, bounded counters, translated strings, and semantic alert behavior. They are the correct shared owner.                     | Keep centralized here.                                                                                            |
| `Org2Cloud/channels/components/ManageChannelMembersDialog.tsx:356`                  | Member-management add action                        | keep with reason | This is an inline sub-form action, not the cancel/confirm footer used by the other eight dialogs. Forcing it into the footer abstraction would create misleading cancel semantics.    | Keep the local `Button`; reuse only the shared error notice.                                                      |
| `DiscussionChannels/ChannelPanelView/ChannelMessageRow.tsx:237`                     | Grouped-message edit/delete toolbar                 | fix              | Grouping suppressed the author header containing the only action controls, leaving later messages in a group uneditable and undeletable.                                              | Landed a focus/hover corner toolbar with the same design-system buttons and accessible labels.                    |
| `DiscussionChannels/ChannelPanelView/index.tsx:196`                                 | Archived local message actions                      | fix              | The composer and drop target were disabled but existing message mutation controls remained active, violating the visible read-only state.                                             | Landed null mutation handlers for archived channels.                                                              |
| `WorkstationSidebarConnector/channelsSection.tsx:168`                               | Cloud channel-bound dialogs                         | fix              | A settings, members, archive, or delete dialog could stay mounted after another instance permanently deleted or revoked access to its channel.                                        | Derive the dialog closed when its target is absent from the authoritative active+archived list.                   |
| `chatPanelTabLifecycleAtoms.ts:204`                                                 | Open discussion-channel tab label and payload       | fix              | A surviving tab retained its cached name/visibility after remote or local rename/archive transitions.                                                                                 | Refresh the title and selected-channel payload from each authoritative listing while preserving the tab identity. |
| `Org2Cloud/channels/components/CreateChannelDialog.tsx`                             | Visibility radio cards                              | keep with reason | PR #610 already added roving tabindex and arrow-key selection while retaining the existing card visual pattern.                                                                       | Keep; covered by dialog tests.                                                                                    |
| `LocalChannels/components/*Dialog.tsx`; `Org2Cloud/channels/components/*Dialog.tsx` | Styling tokens and arbitrary values                 | keep with reason | Changed UI uses existing `Button`, `Checkbox`, `Input`, modal, text, border, fill, danger, and spacing patterns. No new raw color or z-index literal was introduced.                  | No sweep required.                                                                                                |

## Counts

- abstract: 2
- fix: 4
- keep with reason: 4
- unresolved: 0
