# Frontend UI Audit — Data Source Panel

Scope: data-source table alignment with the Settings CLI table, including inline status treatment, auto-scan controls, and row actions.

The repository-referenced `frontend-ui-audit` skill is not installed in either documented location. This report follows the required table convention and manually checks design-system usage, arbitrary Tailwind values, accessibility basics, and visual-pattern duplication.

| Line                            | Element                   | Verdict          | Reason                                                                                                                                                                                                                                                                                                                        | Suggested change                                                                                                                            |
| ------------------------------- | ------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `DataSourcePanel/index.tsx:387` | Source identity cell      | fix              | The dedicated status column separated source state from its identity and did not match the Settings CLI table pattern.                                                                                                                                                                                                        | Render the shared mini `Tag` immediately after the source name and remove the status column.                                                |
| `DataSourcePanel/index.tsx:392` | Status tag variants       | keep with reason | Shared `Tag` colors preserve success, loading, error, and neutral states without introducing a one-off badge treatment; `pill` and `shrink-0` match the CLI table's compact inline tags.                                                                                                                                      | None.                                                                                                                                       |
| `DataSourcePanel/index.tsx:428` | Combined Auto-scan column | fix              | The source switch and refresh action were split across columns, and the row-level open-folder button added a third action that did not match the requested compact CLI-table pattern.                                                                                                                                         | Use one pinned, shared `SETTINGS_TABLE_COL.hug` column ordered as switch, cadence select, refresh; remove the row-level open-folder action. |
| `DataSourcePanel/index.tsx:440` | Source switch size        | keep with reason | The shared `Switch` `default` prop is the canonical 20px-tall variant and matches the Settings CLI table.                                                                                                                                                                                                                     | None.                                                                                                                                       |
| `DataSourcePanel/index.tsx:447` | Cadence selector          | keep with reason | The shared small `Select` provides the intended 28px table-control height, stays adjacent to its enabling switch, and uses a compact 120px trigger while allowing its dropdown to size for content. Its supported `selectorClassName` prop restores left-aligned value text inside the otherwise right-aligned action column. | None.                                                                                                                                       |
| `DataSourcePanel/index.tsx:468` | Rescan split action       | keep with reason | The shared split `Button` uses the requested 28px `small` size while preserving incremental and clear-rescan behavior in one established control.                                                                                                                                                                             | None.                                                                                                                                       |
| `DataSourcePanel/index.tsx:540` | Table width and overflow  | keep with reason | The table keeps the shared `SettingsTable` horizontal-overflow and pinned-last-column behavior; consolidating status and controls leaves the source identity column flexible.                                                                                                                                                 | None.                                                                                                                                       |
| `DataSourcePanel/index.tsx:569` | Shared table header       | fix              | An explicit `headerBorder` override added a header class absent from the Settings CLI table and bypassed its shared default treatment.                                                                                                                                                                                        | Remove the override and use the CLI table's shared expandable-table class props.                                                            |

## Summary

- fix: 3
- keep with reason: 5
- abstract: 0
- sweep candidates: none
