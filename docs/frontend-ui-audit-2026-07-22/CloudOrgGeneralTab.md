# Frontend UI Audit — Cloud Organization General Tab

**Scope:** Consolidation of repository-scope and session navigation into the cloud organization General tab.
**Summary:** 6 fix, 2 keep with reason, 0 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                                         Line | Element                             | Verdict          | Reason                                                                                                                                                             | Suggested change                                                                                                             |
| -------------------------------------------: | ----------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
|                 `CloudOrgPanelHeader.tsx:27` | Management tab switch               | fix              | Repository scopes and Sessions are no longer independent management pages; keeping either tab would duplicate the General and Kanban destinations.                 | Completed: the localized switch now exposes only General and Members, and both dead typed tab variants are removed.          |
|            `CloudOrgPanelView/index.tsx:100` | General-tab composition             | fix              | Repository scope now renders in the same General branch as plan and sharing policy, with organization settings following it.                                       | Completed: mounted `CloudOrgRepoScopesSection` inside General and deleted the parallel tab branch.                           |
|            `CloudOrgSettingsSection.tsx:193` | Sessions settings row               | fix              | Sessions remain a useful organization destination, but a duplicate table inside Manage Org created a second UI for the same projected data.                        | Completed: added one standard `SectionRow` with a localized secondary Open button that selects the org and opens Kanban.     |
| `CloudOrgPanelView/CloudSessionsSection.tsx` | Duplicate Sessions page             | fix              | The page repeated the remote-session roster, replay, and fork controls already owned by Work Management and the cloud action hook.                                 | Completed: deleted the page, its private table mapper, and its mapper test after tracing all production callers.             |
|                   `TaskKanban/index.tsx:176` | Take over action                    | fix              | Deleting the duplicate page would otherwise remove the teammate continuation action.                                                                               | Completed: replayable remote rows expose localized Take over only in Kanban List and call the canonical cloud fork action.   |
|           `CloudOrgRepoScopesSection.tsx:28` | Repository-scope component boundary | keep with reason | Renaming Tab to Section makes ownership accurate while keeping quota, cooldown, admin editing, and member read-only behavior isolated from the panel coordinator.  | None.                                                                                                                        |
|           `CloudOrgRepoScopesSection.tsx:60` | Repository-scope settings card      | keep with reason | Continues using `SectionContainer`, `SectionRow`, `Button`, and the shared `RepoScopePicker`; consolidation adds no custom control or duplicated visual treatment. | None.                                                                                                                        |
|                       `SessionTable.tsx:350` | Shared trailing action column       | fix              | The prior 60px Fork slot was too narrow for localized Take over labels.                                                                                            | Completed: retained propagation isolation and widened the optional action column to 140px; no parallel table was introduced. |

No new raw color, arbitrary Tailwind value, custom button, or duplicate session table was introduced. The systematic sweep removed all production references to the deleted Sessions page, added negative assertions for the dead tab, and moved rendered coverage to General → Kanban List. The existing localized Common Open and Take over labels are reused.
