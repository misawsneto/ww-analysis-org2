# Frontend UI Audit — Cloud Organization Settings Section

**Scope:** Consolidation of plan, minimum sharing level, and organization name into one General-tab settings card.
**Summary:** 4 fix, 3 keep with reason, 0 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                              Line | Element                      | Verdict          | Reason                                                                                                                                                             | Suggested change                                                                                                                                                            |
| --------------------------------: | ---------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CloudOrgSettingsSection.tsx:136` | General settings container   | fix              | Plan, sharing policy, and organization identity previously occupied three separate card paths; one `SectionContainer` now owns the related settings.               | Completed: consolidated the rows under the existing Org settings heading.                                                                                                   |
| `CloudOrgSettingsSection.tsx:139` | Plan row                     | fix              | The old test wrapper sat between `SectionContainer` and `SectionRow`, preventing the shared direct-child separator selector from applying.                         | Completed: moved the stable test ID onto `SectionRow` and set `align="start"` for its optional description.                                                                 |
| `CloudOrgSettingsSection.tsx:190` | Minimum sharing row          | keep with reason | Uses `label`, `description`, `align="start"`, the shared `Select`, and `SECTION_CONTROL_STYLE`; its error remains grouped with the control it describes.           | None.                                                                                                                                                                       |
| `CloudOrgSettingsSection.tsx:230` | Organization name row        | fix              | Rename status and error were previously sibling nodes outside their settings row, which broke row ownership and separator consistency.                             | Completed: grouped input, action, saved state, and error inside one direct `SectionRow`; the label uses the row's default centered alignment because it has no description. |
| `CloudOrgSettingsSection.tsx:271` | Ownership transfer row       | fix              | The transfer error was previously outside the row whose action produced it.                                                                                        | Completed: kept the selector, action, and error together in an `align="start"` row.                                                                                         |
| `CloudOrgSettingsSection.tsx:317` | Danger Zone                  | keep with reason | Destructive deletion remains visually and semantically separate from ordinary settings, while still using `SectionContainer`, `SectionRow`, `Input`, and `Button`. | None.                                                                                                                                                                       |
|        `SectionLayout/Row.tsx:21` | `SectionRow.dataTestId` prop | keep with reason | A row-owned selector preserves direct-child card structure across horizontal, vertical, and content-only layouts without adding wrapper markup.                    | None.                                                                                                                                                                       |

No new raw color, custom control, arbitrary card treatment, or duplicate settings-row component was introduced. The systematic sweep found no alternate cloud organization settings implementation requiring the same consolidation. Existing admin/member and owner-only boundaries remain intact, and focused rendering tests verify that plan, minimum sharing, and organization name share the same direct parent card.
