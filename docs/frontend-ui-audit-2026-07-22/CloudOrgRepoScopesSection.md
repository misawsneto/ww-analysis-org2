# Frontend UI Audit — Cloud Organization Navigation and Repository Scopes

**Scope:** Cloud organization tab ordering and the on-demand repository-scope add flow.
**Summary:** 8 fix, 3 keep with reason, 0 abstract.

> The repository-routed `frontend-ui-audit` skill was unavailable in both documented skill locations. This report is the manual fallback using the required design-system, duplication, arbitrary-style, accessibility, and systematic-sweep checks.

|                                Line | Element                   | Verdict          | Reason                                                                                                                                           | Suggested change                                                                                                                     |
| ----------------------------------: | ------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ |
|        `CloudOrgPanelHeader.tsx:27` | Management tab order      | fix              | The prior General, Sessions, Members order did not match the requested information hierarchy.                                                    | Superseded by the later consolidation: the localized switch now contains only General and Members.                                   |
|        `CloudOrgPanelHeader.tsx:31` | Tab labels                | keep with reason | General continues to use the Settings namespace and Members uses its existing Navigation key; no duplicated or hardcoded labels were introduced. | None.                                                                                                                                |
|  `CloudOrgRepoScopesSection.tsx:44` | Add-flow state            | fix              | The repository inventory mounted and displayed permanently even when the admin was not adding a scope.                                           | Completed: the picker mounts only after the localized Add action and closes after selection.                                         |
| `CloudOrgRepoScopesSection.tsx:152` | Unified action row        | fix              | Add and Save previously occupied separate rows, while Save remained visible without changes.                                                     | Completed: Add stays left when collapsed; localized Cancel and conditional Save share a right-aligned group in the same row.         |
|     `RepoScopePicker/index.tsx:113` | Expanded picker width     | fix              | The repository choices only occupied their content width inside a full-width settings row.                                                       | Completed: the picker root explicitly fills the settings-row width; the temporary Done action is omitted while the picker is open.   |
|      `RepoScopePicker/index.tsx:41` | Add-only selection mode   | fix              | An add surface should not also remove an existing scope; selected repositories now remain visible but disabled.                                  | Completed: added the explicit `addOnly` prop while retaining the picker’s existing multi-select behavior elsewhere.                  |
|     `RepoScopePicker/index.tsx:151` | Repository option content | fix              | Name and remote URL were stacked on two lines, making the add list unnecessarily tall.                                                           | Completed: rendered `repository · remote URL` in one flex row with independent truncation and retained URL tooltip.                  |
|  `CloudOrgRepoScopesSection.tsx:92` | Draft scope rows          | fix              | Draft additions looked committed and draft removals disappeared, obscuring the existing Save boundary.                                           | Completed: marked additions as unsaved, retained removals with strikethrough and Undo, and kept persistence exclusively behind Save. |
| `CloudOrgRepoScopesSection.tsx:168` | Save status               | fix              | A previous Saved badge could remain visible after a new draft edit.                                                                              | Completed: dirty state now takes precedence and shows the localized Unsaved edits status until Save succeeds.                        |
|    `SectionLayout/Container.tsx:20` | Container test selector   | keep with reason | Attaching the stable selector to the card keeps every `SectionRow` as a direct child, preserving the design-system separator contract.           | None.                                                                                                                                |
|  `CloudOrgRepoScopesSection.tsx:79` | Repository settings card  | keep with reason | Continues using `SectionContainer`, direct `SectionRow` children, design-system buttons, semantic status colors, and the established picker.     | None.                                                                                                                                |

No new raw color, custom button, arbitrary card treatment, or duplicate repository picker was introduced. The explanatory note now sits below the settings card instead of consuming a settings row. The systematic sweep updated both rendered cloud E2E flows that previously assumed the picker was permanently visible. Focused tests cover localized tab order, collapsed/expanded picker state, single-line row rendering, staged add/remove behavior, Cancel restoration, conditional Save visibility and alignment, explicit Save persistence, add-only selection, and subscription cleanup.
