# Shared-session polling UI audit

The repository references a user-global `frontend-ui-audit` skill, but it was not installed in this environment. This direct fallback review follows the repository's report-column convention and covers the TSX changes in the Org2Cloud sharing batch.

|                                    Line | Element                         | Verdict          | Reason                                                                                                                                                                                                                                            | Suggested change                                                                                                                                     |
| --------------------------------------: | ------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
|       `CloudOrgPanelView/index.tsx:334` | Roster invalidation effects     | keep with reason | The change splits data ownership without changing markup, tokens, focus behavior, or accessible names. A membership signal now reloads only the members data it invalidates.                                                                      | Keep the existing component and design-system usage; move these loaders into shared per-org query owners if another surface begins duplicating them. |
| `SessionForkHeaderExtras/index.tsx:144` | Imported-session header actions | fix              | The shared-by chip duplicated provenance already visible in the Team Sessions hierarchy and competed with the primary fork action. Removing it simplifies the header without changing the remaining fork control's accessible tooltip or test id. | Keep provenance in the session hierarchy and reserve the header for actionable fork/address controls.                                                |
|                   `PanelContent.tsx:62` | Polling hotspot summary         | fix              | The count announced all likely-polling groups while the grid silently rendered six. Rendering the top six plus every additional likely group makes the diagnostic claim truthful without adding an unlabeled control or a new interaction state.  | If very large captures make the grid unwieldy, add a design-system disclosure button with `aria-expanded`; do not reinstate an unexplained hard cap. |

Summary: 2 fixes, 1 keep with reason, 0 abstractions. No arbitrary Tailwind values, duplicated visual patterns, or new accessibility regressions were introduced.
