# Frontend UI Audit — Cloud Session Reference Chip

**Scope:** `src/features/Org2Cloud/CloudSessionReferenceChip.tsx` (new inline markdown chip for an ORG2 Cloud session reference) and the `a` renderer branch in `src/components/MarkDown/MarkDownImpl.tsx` that instantiates it.
**Summary:** 1 fix, 6 keep with reason, 1 abstract. The `fix` row was applied in this branch (found independently while the audit was running); the row below records the finding as audited, with the resolution noted.

> Neither documented skill location resolved: `.orgii/skills/frontend-ui-audit/SKILL.md` (workspace) and `~/.orgii/skills/frontend-ui-audit/SKILL.md` (user-global) are both absent from this checkout. This report is the manual fallback — a design-system-consistency review applying the same checks the routing table describes (design-system component reuse, arbitrary/hardcoded values, accessibility basics, visual-pattern duplication) and the same table format prior fallback audits in this repo have used (e.g. `docs/frontend-ui-audit-2026-07-23/SharedSessionId.md`).

|                                  Line | Element                                                                   | Verdict          | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Suggested change                                                                                                                                                                                   |
| ------------------------------------: | ------------------------------------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|    `CloudSessionReferenceChip.tsx:20` | `ICON_PROPS = { size: 12, "aria-hidden": true }`                          | fix              | `12` is a hardcoded value, not the design-system token. Every existing BasePill consumer sizes its icon from `PILL_SIZE.iconSize` (14px) and pairs it with `strokeWidth: 1.75` — see `UserMessageContent.tsx:200` (`{ size: PILL_SIZE.iconSize, strokeWidth: 1.75 }`) and `ComposerPill.tsx:54` (identical). This chip's icon is both smaller than the token and, lacking `strokeWidth`, renders at lucide's default weight (2) instead of the house style, so it reads visually off from the two precedents it's supposed to match.                                                                                                                                                                                                                                                            | **APPLIED.** `const ICON_PROPS = { size: PILL_SIZE.iconSize, strokeWidth: 1.75, "aria-hidden": true } as const;` with `PILL_SIZE` imported from `@src/config/pillTokens`.                          |
|    `CloudSessionReferenceChip.tsx:21` | `SHORT_ID_LENGTH = 8`                                                     | keep with reason | Not a style/spacing/color value — `pillTokens.ts` governs visual tokens, not label-content truncation length, which is a product decision about how much of the id to surface. Locally scoped, used once; nothing to abstract.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | None.                                                                                                                                                                                              |
|    `CloudSessionReferenceChip.tsx:49` | `shortId = reference.sourceSessionId.slice(-8)`                           | keep with reason | Truncation is display-only and safe. `.slice(-8)` degrades gracefully on shorter ids (no throw, returns the whole string). The click handler (line 36) passes the **full** `reference` object — including the untruncated `sourceSessionId` — to `openReference`, so no routing/identity logic depends on the shortened value. The reference itself carries no capability (per `cloudSessionReference.ts`'s docblock: this is `/session/ref`, not the capability-bearing `/session?share=`), so an 8-char suffix collision at worst mislabels a chip visually — it can't misdirect access, since every read is re-checked server-side by org membership regardless of which id string a user sees.                                                                                              | None.                                                                                                                                                                                              |
|    `CloudSessionReferenceChip.tsx:50` | `` `${t("cloud.sessionRef.chipLabel")} ${shortId}` ``                     | keep with reason | Raw concatenation of a translated string with an appended literal, rather than an i18n interpolation placeholder (e.g. `t(key, { shortId })`) that would let each locale file control word order. This is a real i18n limitation but not a new pattern: `CloudSessionShareDialog/index.tsx:187` already does the identical "translated label + literal id suffix" concatenation (`` `${t("cloud.share.linkShareLabel")} #${share.id.slice(-4)}` ``). `navigation:cloud.sessionRef.chipLabel` was confirmed present as a plain string (no `{{}}` placeholder) in all 13 locales (en, zh, zh-Hant, ja, ko, de, es, fr, pt, ru, tr, pl, vi); none are RTL, so bidi concerns don't apply.                                                                                                           | None for this PR. If a future sweep touches `linkShareLabel` too, consider a shared `t(key, { suffix })` interpolation convention for "label + id" pills.                                          |
| `CloudSessionReferenceChip.tsx:32-47` | `handleClick` / `handleKeyDown` keyboard glue                             | abstract         | The "preventDefault + stopPropagation" click guard paired with an "Enter/Space triggers click" keydown handler is now hand-written in at least two BasePill consumers: this chip and `InlinePill` (`UserMessageContent.tsx:306-384`, specifically its `handleKeyDown` at 378-384). Since BasePill is the shared shell for exactly this "clickable inline pill" contract, this is a candidate for a small shared hook (e.g. `useBasePillActivation(onActivate): { onClick, onKeyDown }`) so future BasePill consumers stop re-deriving the same handful of lines.                                                                                                                                                                                                                                | Flagging as a sweep candidate per the audit's systematic-sweep discipline — not fixed here. Land a shared hook only if the user wants a cross-file refactor; don't fix it site-by-site in this PR. |
| `CloudSessionReferenceChip.tsx:56-61` | `role="button" tabIndex={0} title cursor onClick onKeyDown` on `BasePill` | keep with reason | Matches `InlinePill`'s clickable contract at `UserMessageContent.tsx:415-427` (`role={isClickable ? "button" : undefined}`, `tabIndex={isClickable ? 0 : undefined}`, `title={segment.displayName}`, `cursor: isClickable ? "var(--interactive-cursor, default)" : "default"`), simplified because this chip has no non-interactive state — it is always clickable, so the ternaries collapse to unconditional values and the cursor fallback is `pointer` instead of `default`. No focus-visibility regression: the global reset that strips `outline: none` from anchors (`src/styles/_common.scss:128-137`) targets `a`/`a:focus` only, and this chip renders a `<span>`, so the browser's default focus ring is preserved on tab-focus.                                                     | None.                                                                                                                                                                                              |
| `CloudSessionReferenceChip.tsx:52-65` | Root element is `<BasePill>`, no wrapping element                         | keep with reason | Satisfies the inline-element requirement: `BasePill`'s shell is `display: inline-flex` (`PILL_BASE_STYLE` in `pillTokens.ts:176-190`), and the component returns it directly with no enclosing `div`/`p`, so it composes safely inside a markdown `<p>`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | None.                                                                                                                                                                                              |
|            `MarkDownImpl.tsx:604-609` | `a()` renderer chip branch                                                | keep with reason | Replacing the native `<a>` with a `role="button"` span (instead of e.g. `role="link"`) matches the codebase's existing convention: `InlinePill` uses `role="button"` uniformly for every pill type that opens something on click, including its own `isGitHubUrl` case that opens an external URL (`UserMessageContent.tsx:311-314`) — so this isn't a new semantic choice, it's consistent with how the app already treats "click this inline chip to go somewhere." The branch intentionally drops `children`/`...props` instead of forwarding them: the discarded children are just the literal `orgii://...` reference text `remarkCloudSessionReferences.ts` wrapped into a link node, which the chip was never meant to display — it renders its own translated, localized label instead. | None.                                                                                                                                                                                              |

No arbitrary Tailwind values were introduced — `CloudSessionReferenceChip.tsx` uses no Tailwind classes at all; all styling flows through `BasePill`'s token-driven inline styles (`pillTokens.ts`), the same mechanism `InlinePill` uses. The one real deviation from the design system is the icon sizing/stroke-width (fix row above); everything else in the new component and the `MarkDownImpl.tsx` edit either matches or correctly simplifies the `InlinePill` precedent it was built to resemble. The systematic sweep for duplicated visual/behavioral patterns found one candidate worth surfacing rather than fixing inline: the Enter/Space keyboard-activation glue duplicated between this chip and `InlinePill`.
