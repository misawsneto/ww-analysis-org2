# Frontend UI Audit — PrCommitsTab

Scope: the pull-request Commits tab and its handoff from `PrDetailPanel`.

| Line | Element                              | Verdict          | Reason                                                                                                                                                                           | Suggested change                                                                                          |
| ---- | ------------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 186  | Commit result container              | fix              | The former flat 36px history rows did not match the full-card activity treatment used by PR Conversation and obscured useful GitHub metadata.                                    | Replaced with bordered `bg-primary-container` blocks and preserved inline detail navigation.              |
| 207  | Commit author avatar                 | keep with reason | The shared `Avatar` component supplies the same sizing, fallback, and circular treatment used by Conversation actors.                                                            | Keep the shared component; do not introduce commit-specific image styling.                                |
| 242  | SHA and detail actions               | keep with reason | `ActivityHeaderActionButton` is the canonical compact action used by activity surfaces and provides consistent hover and accessible-label behavior.                              | Keep both actions on the shared primitive.                                                                |
| 198  | Commit title and metadata typography | keep with reason | The 13px title over 12px metadata matches the existing activity-card hierarchy while retaining more information than the old compact row.                                        | Keep until these established activity typography values are promoted to named tokens.                     |
| 376  | Date-group timeline marker and rail  | keep with reason | The shared timeline primitives model card-to-card conversation events, not a date header followed by an indented batch of commit cards. A local rail avoids distorting that API. | Keep local to `PrCommitsTab`; abstract only if another batched activity surface needs the same structure. |
| 187  | Primary card interaction             | keep with reason | A real button provides keyboard access to inline commit details while keeping copy/detail actions as valid sibling buttons instead of nesting controls.                          | Keep the split interactive structure.                                                                     |

Verdict totals: **fix 1 · keep with reason 5 · abstract 0**.
