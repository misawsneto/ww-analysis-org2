# Frontend UI Audit — Reusable Markdown Composer

**Files:** `src/components/ComposerBar/index.tsx`, `src/components/ComposerSurface/index.tsx`, `src/features/Org2Cloud/CloudSessionReferencePreview.tsx`, `src/modules/shared/components/RichMarkdownEditor/index.tsx`, `src/modules/ProjectManager/shared/components/CreateComposerScaffold.tsx`, `src/modules/ProjectManager/shared/components/ProjectContentEditor/index.tsx`, `src/modules/ProjectManager/WorkItems/components/CreateWorkItemView/InlineCreateWorkItemFields.tsx`, `src/modules/ProjectManager/WorkItems/components/WorkItemContent/GitHubIssueComposer.tsx`, and `src/modules/WorkStation/CodeEditor/Panels/EditorPrimarySidebar/content/PullRequestContent/detail/PrConversationTab.tsx`
**Date:** 2026-08-09

## D1 — Raw HTML vs Design System

| Line                                           | Element                                 | Verdict  | Reason                                                                                                                                                                                                    | Suggested change                                                                                                        |
| ---------------------------------------------- | --------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `ComposerSurface/index.tsx:15–67`              | Editor shell and action-row composition | abstract | Create, issue, and PR composers all need the established `ComposerShell` padding and `ComposerBar` left/right action geometry. A shared surface prevents their footer insets from drifting independently. | Route editor-backed composers through `ComposerSurface`; keep domain-specific editor content and actions in the caller. |
| `CreateComposerScaffold.tsx:123–135`           | Manual Project/Work Item composer       | fixed    | The manual creator previously assembled `ComposerShell` and `ComposerBar` directly, leaving review/comment surfaces without a reusable equivalent.                                                        | Consume `ComposerSurface` while preserving mention, upload, skill, and submit controls.                                 |
| `GitHubIssueComposer.tsx:92–156`               | GitHub issue comment input              | fixed    | The issue footer hand-coded an 8px horizontal inset and a taller button, so its submit control did not match the session composer.                                                                        | Use shared shell padding, shared action-row padding, and the standard small action-button height.                       |
| `PrConversationTab.tsx:453–506`                | PR comment/review input                 | fixed    | The PR footer hand-coded a 16px inset around Submit review and Comment, diverging from both session and issue composers.                                                                                  | Provide the two domain actions through `ComposerSurface` leading/trailing slots.                                        |
| `CloudSessionReferencePreview.tsx:35–75`       | Optional PR reference preview           | fixed    | A caller-owned wrapper would remain in the flex layout even when the preview rendered no references, introducing an extra composer gap.                                                                   | Forward an optional class to the preview's rendered element so the absent state creates no layout node.                 |
| `ProjectContentEditor/index.tsx:49–76,379–406` | Markdown formatting controls            | abstract | The reusable content editor already owns Markdown serialization but exposed only its selection toolbar. Composer contexts need an explicit inline formatting mode.                                        | Keep floating mode as the default and expose an opt-in inline mode using the shared composer-toolbar class.             |
| `InlineCreateWorkItemFields.tsx:455–474`       | Manual Create Work Item description     | fixed    | The manual description stored Markdown but did not show the always-available formatting controls used by comment/review composers.                                                                        | Select inline toolbar mode for Manual and retain floating mode for Agent generation.                                    |

## D2 — Arbitrary Tailwind Value vs Token

| Line                                 | Value                        | Verdict          | Reason                                                                                                                                                                             | Suggested change                                                                       |
| ------------------------------------ | ---------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `RichMarkdownEditor/index.tsx:16–18` | Inline toolbar class         | keep with reason | The existing review toolbar sizing is now named once and reused by issue, PR, and manual Work Item Markdown editors, avoiding repeated arbitrary SVG sizing and padding overrides. | Keep future composer-toolbar geometry changes at this shared constant.                 |
| `ComposerSurface/index.tsx:53–66`    | Shell and action-row spacing | keep with reason | Spacing is inherited from `ComposerShell` and `ComposerBar`, which are the established session-input owners. No new one-off composer padding value is introduced.                  | Continue consuming those primitives rather than adding caller-level padding overrides. |

## D4 — Accessibility

| Line                   | Element                    | Verdict          | Reason                                                                                                                                                                                                                   | Suggested change |
| ---------------------- | -------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| All migrated composers | Input and action semantics | keep with reason | Native buttons, disabled/loading behavior, section labels, editor keyboard submission, and forwarded drop-target refs remain intact; the change only consolidates presentation and exposes existing formatting controls. | —                |

## Summary

- 5 fixes applied
- 2 abstractions implemented
- 3 kept with documented reason
