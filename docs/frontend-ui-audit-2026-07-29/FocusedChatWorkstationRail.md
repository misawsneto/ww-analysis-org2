# Frontend UI Audit — Focused Chat Workstation Rail

Scope: the maximized chat pane's responsive workstation environment rail, its compact pinned-menu replacement, fixed two-column content layout, and exact-branch GitHub compare/PR CI status.

The repository-referenced `frontend-ui-audit` skill is not installed in either documented location. This report follows the required table convention and manually checks design-system usage, arbitrary Tailwind values, accessibility, responsive behavior, and duplicated visual patterns.

| Line                                 | Element                      | Verdict          | Reason                                                                                                                                                                                                                                                                                                                                 | Suggested change |
| ------------------------------------ | ---------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `ChatPanelShell.tsx:70`              | Named chat container         | keep with reason | The breakpoint follows the actual maximized chat width rather than the viewport, so navigation/sidebar width cannot incorrectly select the rail layout.                                                                                                                                                                                | None.            |
| `ChatPanelShell.tsx:86`              | Two-column chat body         | fix              | The prior rail floated over the transcript. Chat history and composer now share one flexible left pane while the environment trail occupies a fixed right-side layout track.                                                                                                                                                           | Completed.       |
| `FocusedChatWorkstationRail.tsx:702` | 1100px rail breakpoint       | keep with reason | At this threshold, the fixed 256px rail occupies layout space while the flexible main pane takes the remaining width. Below it, preserving the main pane takes priority and the compact dropdown replaces the rail.                                                                                                                    | None.            |
| `ChatPanelHeader.tsx:187`            | Compact menu host            | abstract         | One shell-owned header slot places the narrow control in the existing action group—after session extras such as Notes and immediately before More—without teaching the shared header about repository, terminal, tab, or Git state.                                                                                                    | None.            |
| `FocusedChatWorkstationRail.tsx:208` | Shared action row            | abstract         | Wide rail and compact dropdown share one native-button row implementation, existing dropdown item tokens, file-type icons, diff stats, keyboard hints, external-link affordances, and CI status. Wide tab rows own one hover surface containing both the tab action and its hover-revealed close affordance.                           | None.            |
| `FocusedChatWorkstationRail.tsx:321` | Shared CI status renderer    | abstract         | One compact status renderer maps exact-branch PR checks to the existing success, warning, danger, and neutral tokens in both wide and dropdown presentations; the collapsed presentation derives its dot from the same state, and all visible status text is localized across the 13 supported languages.                              | None.            |
| `FocusedChatWorkstationRail.tsx:377` | Shared section renderer      | abstract         | Repository, branch, and environment actions render first with equal row gaps, followed by Open Tabs. The same section model feeds both responsive presentations, so there is no second action model to drift.                                                                                                                          | None.            |
| `FocusedChatWorkstationRail.tsx:617` | Compare branch action        | keep with reason | GitHub repositories expose a Codex-style external Compare branch row using the exact default and current branch refs plus the shared current-color GitHub brand SVG. Non-GitHub folders do not receive a misleading action, and the label is localized across every supported language.                                                | None.            |
| `FocusedChatWorkstationRail.tsx:628` | Linked PR CI status          | fix              | An open PR appears only when GitHub resolves the exact current branch; its head SHA supplies passed, running, failed, empty, or unavailable CI state. The PR row opens the linked PR, while the collapsed icon keeps the state in its accessible name and a token-colored dot.                                                         | Completed.       |
| `FocusedChatWorkstationRail.tsx:700` | Pinned Environment dropdown  | fix              | Below 1100px the vertical trail is removed and replaced with a toolbar-pinned icon and a Codex-style Environment panel using the existing dropdown surface, item, focus, and keyboard-navigation system.                                                                                                                               | Completed.       |
| `FocusedChatWorkstationRail.tsx:729` | Compact trigger semantics    | keep with reason | The trigger uses the same tertiary small design-system button as the neighboring header actions, with a localized label, `aria-expanded`, and `aria-haspopup`; compact action rows expose menu-item roles and remain keyboard reachable.                                                                                               | None.            |
| `FocusedChatWorkstationRail.tsx:752` | Fixed Environment column     | fix              | The wide presentation reserves a non-resizable 256px layout track with an 8px top inset and 4px side/bottom insets, moving the complete rail stack down 4px while preserving the original rounded bordered card, compact row geometry, canvas surface, and scroll containment.                                                         | Completed.       |
| `FocusedChatWorkstationRail.tsx:774` | Environment collapse control | keep with reason | The wide card restores its original explicit expand/collapse behavior and persisted preference, switching only between fixed 256px and 44px layout tracks. The collapsed card is 36px wide, and its toggle and actions share one 26px button class with larger vertical gaps; no drag handle or continuous resize state is introduced. | None.            |
| `FocusedChatWorkstationRail.tsx:831` | Conversation minimap host    | fix              | The 36px marker-only conversation navigator is portaled 8px beneath the workstation card. Its fixed trailing host stays centered on the collapsed rail position when the card expands, while the compact anchor preserves chat-edge placement without reserving a hidden rail column.                                                  | Completed.       |
| `FocusedChatWorkstationRail.tsx:451` | Working-tree diff totals     | keep with reason | The component still consumes the shared single-flight, visibility-aware numstat store, and it remains mounted only while a focused session owns the controls.                                                                                                                                                                          | None.            |

## Summary

- fix: 5
- keep with reason: 6
- abstract: 4
- sweep candidates: none
