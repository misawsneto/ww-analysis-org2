# SessionInfoLine UI audit

| Line                                                                                 | Element                           | Verdict          | Reason                                                                                                                                                                                                                         | Suggested change |
| ------------------------------------------------------------------------------------ | --------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| `src/features/SessionCreator/components/SessionInfoLine.tsx:134`                     | `leadingContent` slot             | keep with reason | The GUI/TUI segmented control has different interaction and selected-state semantics from a selector segment, so a typed leading slot preserves the shared `PillGroup` instead of forcing it into an incompatible segment API. | None.            |
| `src/features/SessionCreator/components/SessionInfoLine.tsx:620`                     | Divider after the leading control | keep with reason | This is the existing `border-2` design token and separates the selector group from a control that owns its own fill and active state.                                                                                          | None.            |
| `src/features/SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:196` | CLI launch-mode composition       | keep with reason | The view already owns both the session-info line and the conditional CLI-only control, making it the narrowest placement boundary without coupling `SessionInfoLine` to launch-mode state.                                     | None.            |

Verdict totals: **0 fix**, **3 keep with reason**, **0 abstract**.
