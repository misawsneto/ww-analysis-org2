# PrDetailPanel UI audit

Scope: the shared pull-request detail surface and its Station, Chat, and Team Inbox hosts.

| Line                                | Element                                | Verdict          | Reason                                                                                                                                                                                                                | Suggested change |
| ----------------------------------- | -------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `PrDetailPanel.tsx:164`             | Pull-request tab navigation            | keep with reason | The shared `DetailTabStrip` owns the tab buttons, active state styling, counts, and tab semantics across every PR host.                                                                                               | None.            |
| `PrDetailPanel.tsx:354`             | Panel-owned tab row                    | keep with reason | Non-Station hosts render the same shared tab component directly, avoiding per-host title-plus-tab shells or duplicated navigation markup.                                                                             | None.            |
| `PrDetailPanel.tsx:374`             | Persistent tab panels and details rail | keep with reason | The flex layout is application-specific: it preserves mounted tab state and co-locates the PR controls with the workstation navigation trail, neither of which is representable by a general design-system primitive. | None.            |
| `PrConversationTab.tsx:345`         | Conversation content surface           | keep with reason | The conversation no longer accepts a narrow-layout sidebar slot; its custom scroll surface now has one stable composition beneath the flow header.                                                                    | None.            |
| `githubPrDetail.tsx:77`             | Workstation header tabs                | keep with reason | This is the tab-host publication boundary; it embeds `PrDetailTabs` rather than recreating PR title or navigation chrome.                                                                                             | None.            |
| `TeamInboxDetailPane.tsx:76`        | Team Inbox trailing tab actions        | keep with reason | Team Inbox supplies its existing accessible `TeamInboxHeaderIconAction` controls as trailing tab actions, retaining the host-specific open behaviors without a second header.                                         | None.            |
| `DetailTabStrip.tsx:46`             | 40px host-header tab alignment         | keep with reason | The header variant uses the shared 40px header height so the selected tab reaches and masks the shell divider instead of leaving a gap below the pill.                                                                | None.            |
| `GitHubDetailSkeleton/index.tsx:41` | Host-owned PR tab loading state        | keep with reason | The shared skeleton can omit its decorative tab placeholders when My Station already owns the real tab row, preventing a stale duplicate row while retaining the same loading body.                                   | None.            |

Verdict totals: **0 fix**, **8 keep with reason**, **0 abstract**.
