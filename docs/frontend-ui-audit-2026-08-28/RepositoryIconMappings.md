# RepositoryIconMappings UI audit

| Line                                                                               | Element                                 | Verdict          | Reason                                                                                                                                                                                                                       | Suggested change |
| ---------------------------------------------------------------------------------- | --------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/modules/MainApp/SelectRepo/index.tsx:126`                                     | Create-workspace action icon            | keep with reason | Uses the established `HugeiconsIcon` renderer and the dedicated `FolderLibraryIcon` glyph; the action creates a multi-repo workspace                                                                                         | None             |
| `src/modules/MainApp/SelectRepo/index.tsx:266`                                     | Saved workspace row icon                | keep with reason | The row is a custom onboarding-list item, while the icon is semantic and matches the shared multi-repo vocabulary                                                                                                            | None             |
| `src/modules/MainApp/SelectRepo/index.tsx:298`                                     | Repository-kind icon branch             | keep with reason | The `REPO_KIND.FOLDER` branch is the owning domain discriminator, so keeping the mapping at this display boundary prevents a Git glyph for a non-Git folder                                                                  | None             |
| `src/modules/WorkStation/shared/StatusBar/components/EditorStatusBarLeft.tsx:126`  | Active workspace/repository status icon | keep with reason | The status bar receives only `isMultiRoot` and `isGitInitialized`; its inline mapping preserves the established code glyph for Git repositories while distinguishing the other two states without a new cross-layer icon API | None             |
| `src/scaffold/GlobalSpotlight/palettes/WorkspacePalette/WorkspaceDropdown.tsx:115` | Dropdown repository-kind icon branch    | keep with reason | This compact dropdown has its own `RepoItem` renderer, and now uses the same authoritative `REPO_KIND.FOLDER` distinction as the full selector                                                                               | None             |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.
