# Git Profile Sidebar UI Audit

Scope: the reusable settings sidebar/detail layout introduced for Git Profiles. The configured `frontend-ui-audit` skill was not available in either documented location, so this is a manual audit using the repository-required output columns.

| Line                                                                | Element                  | Verdict          | Reason                                                                                                                                                  | Suggested change                                                               |
| ------------------------------------------------------------------- | ------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `src/modules/shared/layouts/SectionLayout/SidebarSplit.tsx:12`      | `SectionSidebarSplit`    | abstract         | The responsive 240px sidebar, border transition, padding, and content column are generic settings-layout concerns rather than Git Profile behavior.     | Keep the shell in `SectionLayout` and reuse it for comparable sidebar editors. |
| `src/modules/shared/layouts/SectionLayout/SidebarSplit.tsx:42`      | `SectionSidebarList`     | abstract         | Centralizing vertical spacing prevents each settings page from rebuilding its own sidebar stack.                                                        | Keep as the standard list wrapper for this shell.                              |
| `src/modules/shared/layouts/SectionLayout/SidebarSplit.tsx:60`      | `SectionSidebarItem`     | abstract         | Selection, hover, disabled, leading/trailing slots, and button semantics are reusable across navigation and sidebar actions.                            | Keep item visuals and behavior centralized.                                    |
| `src/modules/shared/layouts/SectionLayout/SidebarSplit.tsx:73`      | Selected-state semantics | keep with reason | `aria-pressed` is emitted only when callers explicitly pass selection state, so navigation rows expose state while Add/Refresh remain ordinary actions. | No change.                                                                     |
| `src/modules/shared/layouts/SectionLayout/SidebarSplit.tsx:77`      | Selected background      | fix              | The former Git-specific row used `bg-primary-1` and primary text. The shared row now uses the requested neutral `bg-bg-2` with semantic text tokens.    | Fixed in this change.                                                          |
| `src/modules/MainApp/Integrations/Git/Table/GitProfilesTab.tsx:288` | Add action               | fix              | Add previously shared a compact horizontal action group with Refresh. It is now an independent full-width sidebar row.                                  | Fixed in this change.                                                          |
| `src/modules/MainApp/Integrations/Git/Table/GitProfilesTab.tsx:295` | Refresh action           | fix              | Refresh is now independently discoverable, labeled, disabled while active, and retains a spinning progress icon.                                        | Fixed in this change.                                                          |
| `src/modules/MainApp/Integrations/Git/Table/GitProfilesTab.tsx:266` | Active profile indicator | keep with reason | The green check communicates which profile matches global Git configuration independently from the neutral selected editing row.                        | No change.                                                                     |

Verdict summary: **3 fix**, **2 keep with reason**, **3 abstract**.

No arbitrary color values or duplicated responsive sidebar classes remain in the Git Profile component. No wider token/config sweep is required.
