# Frontend UI Audit — Git Profiles

Scope: `GitTable.tsx`, `GitProfilesTab.tsx`, and `gitProfiles.ts`.

The referenced `frontend-ui-audit` skill was not available in either repository-documented location. This report follows the repository-required table convention and manually checks design-system usage, arbitrary Tailwind values, accessibility, responsive behavior, and duplicated visual patterns.

| Line                     | Element                         | Verdict          | Reason                                                                                                                                                                                                                                                      | Suggested change                                                                                                |
| ------------------------ | ------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `GitTable.tsx:255`       | Settings tabs                   | keep with reason | Reuses the established `TabPill` header pattern and gives the two settings surfaces equal navigation weight.                                                                                                                                                | None.                                                                                                           |
| `GitProfilesTab.tsx:219` | Profile section shell           | keep with reason | Reuses `SectionContainer` as a compact, headerless settings surface rather than introducing a new card primitive.                                                                                                                                           | None.                                                                                                           |
| `GitProfilesTab.tsx:266` | Add profile action              | keep with reason | Lives with the profile choices in the sidebar and uses the shared `Button` component, a visible label, an icon, and a stable test id.                                                                                                                       | None.                                                                                                           |
| `GitProfilesTab.tsx:243` | Responsive master-detail layout | keep with reason | The `360px`, `720px`, and `240px` values encode this component's specific minimum editor height, collapse breakpoint, and profile-list width; no matching design token exists. At narrow widths the list becomes horizontal and the editor stacks below it. | Keep local until the same profile-manager pattern exists in another feature; then promote shared layout tokens. |
| `GitProfilesTab.tsx:246` | Profile selector rows           | keep with reason | Native buttons provide keyboard focus and activation semantics; selection is conveyed with both color and content, while active state includes an accessible check label.                                                                                   | None.                                                                                                           |
| `GitProfilesTab.tsx:278` | Identity form                   | keep with reason | Uses shared `SectionRow`, `Input`, `Select`, and `Switch` components. Required fields are visibly marked, and the email picker preserves manual entry.                                                                                                      | None.                                                                                                           |
| `GitProfilesTab.tsx:349` | Profile actions                 | keep with reason | Uses shared action spacing and shared buttons; destructive action uses the existing confirmation helper and is hidden when the final profile cannot be removed.                                                                                             | None.                                                                                                           |
| `GitProfilesTab.tsx:383` | Raw config editor               | keep with reason | Uses the shared `Textarea`, a monospace treatment appropriate for config text, and a separate apply action so typing cannot silently mutate Git config.                                                                                                     | None.                                                                                                           |

## Summary

- fix: 0
- keep with reason: 8
- abstract: 0
- sweep candidates: none
