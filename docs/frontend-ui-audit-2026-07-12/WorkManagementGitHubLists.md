# Kanban GitHub lists — frontend UI audit

Scope: shared GitHub-style issue/PR rows, summary headers, pagination, and view-format sidebar presets in Kanban.

| Line                                                                           | Element                    | Verdict          | Reason                                                                                                                                                                                                                              | Suggested change                                             |
| ------------------------------------------------------------------------------ | -------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| `GitHubWorkItemList.tsx:8-72`                                                  | Toolbar actions            | abstract         | Open, optional create, and refresh actions use one Button-based implementation in the shared Issues/PR search toolbar.                                                                                                              | Keep scope-specific create behavior in the caller.           |
| `GitHubWorkItemList.tsx:81-111`                                                | Sidebar preset list        | abstract         | Reuses the shared `TreeRowBase` primitive used by Kanban Views, so row height, typography, icons, hover, focus, and selected styling stay consistent.                                                                               | Keep GitHub-specific preset semantics outside `TreeRowBase`. |
| `GitHubWorkItemList.tsx:122-150`                                               | Open/closed summary        | abstract         | Issues and PRs share the same count-header structure while supplying their own state icons, labels, and query handlers.                                                                                                             | Keep as an Kanban GitHub-domain primitive.                   |
| `GitHubWorkItemList.tsx:152-190`                                               | List frame and row shell   | abstract         | The bordered list surface and compact row geometry are shared without forcing issue labels or PR branch metadata into one overloaded model.                                                                                         | Continue passing domain-specific content through slots.      |
| `GitHubWorkItemList.tsx:192-240`                                               | Pagination footer          | abstract         | Uses the existing Button primitive and centralizes Previous/Next layout and disabled/loading behavior for both lists.                                                                                                               | Keep page arithmetic in the tested pure helper module.       |
| `GitHubWorkItemsSurface.tsx:813-916`                                           | Issue row                  | keep with reason | Preserves issue-only state, labels, comments, avatar, and browser actions while using the shared row shell. Decorative avatar remains correctly empty-alt because author text is already visible.                                   | None.                                                        |
| `GitHubWorkItemsSurface.tsx:918-978`                                           | Pull request row           | keep with reason | Matches issue-row hierarchy while retaining PR-only draft and branch information; unavailable avatar/comment fields are not fabricated.                                                                                             | Enrich only if the PR API later exposes those fields.        |
| `GitHubWorkItemsSurface.tsx:1259-1323`                                         | Sidebar preset state       | keep with reason | Assigned, authored, closed, and all-state presets mutate the canonical search query and clear incompatible qualifiers, avoiding parallel filter state. PR presets are omitted because the current API loads open PRs only.          | None.                                                        |
| `GitHubWorkItemsSurface.tsx:1860-1949`                                         | Virtualized results        | keep with reason | Both scopes use the same frame/header/row primitives while retaining virtualization and issue-specific remote continuation.                                                                                                         | None.                                                        |
| `GitHubWorkItemsSurface.tsx:2049-2053` and `WorkManagementSidebar.tsx:331-390` | Issues sidebar composition | keep with reason | The sidebar portal contains only `TreeRowBase` filter rows without a custom inset wrapper, and it mounts as a normal `flexGrow: 1` section—the same height allocation and row geometry as Views.                                    | None.                                                        |
| `WorkManagementSidebar.tsx:331-345` and `GitHubWorkItemsSurface.tsx:2084-2134` | Search-toolbar actions     | keep with reason | PRs omit the unnecessary second-level section, while both Issues and PRs place open/refresh actions beside search; Issues additionally expose Create there. The redundant result-count text remains removed.                        | None.                                                        |
| `GitHubWorkItemsSurface.tsx:1156-1213` and `WorkManagement/index.tsx:56-126`   | Detail sidebar policy      | keep with reason | Issue and PR detail state is reported to the shell, which derives a collapsed sidebar and disables the existing toggle without overwriting the user's persisted sidebar preference. Returning to the list restores the prior state. | None.                                                        |
| GitHub reference screenshot                                                    | Bulk-selection checkbox    | keep with reason | The surface has no valid bulk action, so omitting a dead checkbox preserves accessibility and avoids misleading affordance.                                                                                                         | Add selection only with a real bulk operation.               |

Verdict counts: **fix 0**, **keep with reason 8**, **abstract 5**.

Accessibility check: result titles remain native buttons with descriptive names; row actions are siblings rather than nested buttons; summary controls are native buttons; decorative avatars have empty alt text; sidebar presets inherit `TreeRowBase` focus and selected behavior; pagination uses localized labels and disabled states; the sidebar toggle remains visible but uses its native disabled state during detail views.

Validation note: Prettier, targeted ESLint, pagination unit tests, the full TypeScript check, and whitespace validation pass. Per user preference, no browser automation was used.
