# Kanban GitHub sidebar consolidation — frontend UI audit

Scope: removal of the GitHub-only secondary rail and consolidation of repository, filter, and action controls into the existing Kanban primary sidebar.

| Line                                   | Element                                 | Verdict          | Reason                                                                                                                                                                                                                      | Suggested change                                                                    |
| -------------------------------------- | --------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `WorkManagementSidebar.tsx:331-355`    | Conditional GitHub controls section     | keep with reason | Reuses `PrimarySidebarLayout`'s existing global-section pattern, so the controls sit below the established Manage Items and Views sections without introducing another sidebar primitive.                                   | None.                                                                               |
| `WorkManagementSidebar.tsx:410-419`    | Primary sidebar composition             | keep with reason | The GitHub section only mounts for GitHub Issues or PRs and remains part of the same collapsible sidebar hierarchy.                                                                                                         | None.                                                                               |
| `GitHubWorkItemsSurface.tsx:1939-2034` | Repository, filter, and action controls | keep with reason | Preserves the design-system `Select`, `TabPill`, and `Button` controls, existing localization, keyboard behavior, and accessible labels.                                                                                    | None.                                                                               |
| `GitHubWorkItemsSurface.tsx:2036-2088` | Full-width list/detail surface          | fix              | The redundant fixed-width secondary rail duplicated the primary sidebar and reduced result-list width.                                                                                                                      | Removed the rail and allowed the list/detail surface to own the full content width. |
| `GitHubWorkItemsSurface.tsx:2090-2104` | Sidebar portal                          | abstract         | A portal keeps GitHub filter state and handlers owned by the GitHub surface while rendering the controls in the canonical sidebar section. This avoids duplicating state through a second store or parallel component tree. | Keep the portal target scoped to Kanban.                                            |
| `index.tsx:48-99`                      | Sidebar host ownership                  | keep with reason | The Kanban page owns one stable host ref shared by its sidebar and active GitHub surface, with no global DOM identifier or cross-window dependency.                                                                         | None.                                                                               |

Verdict counts: **fix 1**, **keep with reason 4**, **abstract 1**.

Accessibility check: repository and filter controls retain their native/design-system semantics; icon-only actions retain localized `aria-label` values; the new section uses the existing collapsible sidebar header behavior. No new arbitrary colors, shadows, or duplicate interaction primitives were introduced.

Validation note: Prettier, targeted ESLint, the ChatPanel tab DOM regression test, and the full TypeScript check pass. The Tauri development app remained running through the hot reload.
