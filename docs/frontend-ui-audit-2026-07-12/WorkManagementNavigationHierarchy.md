# Kanban navigation hierarchy — frontend UI audit

Scope: expose Kanban as a top-level app-sidebar destination, consolidate project and issue destinations under the localized Work Items group, hide sessions while Work Items is expanded, remove the duplicate in-page sidebar, and place the Kanban/List/Diary presentation switch in the shared 40px header.

| Line                                            | Element                                                | Verdict          | Reason                                                                                                                                                                                                                                   | Suggested change                                                                                                 |
| ----------------------------------------------- | ------------------------------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `workstationSidebarMenuItems.tsx:60-81`         | Top-level Kanban and expandable Work Items rows        | fix              | Kanban is a primary working surface, while Projects, GitHub Issues, and GitHub PRs form a related work-management group. The duplicate standalone Work Items row is removed.                                                             | Keep Kanban top-level and attach the management destinations as Work Items children.                             |
| `workItemsSidebarMenuItems.tsx:25-69`           | Expanded Work Items destinations and selection mapping | keep with reason | Reuses the shared `NavigationMenuItem` child-row model, Lucide icon family, compact rendering, localization, search filtering, and existing expand/collapse affordance instead of introducing bespoke sidebar chrome.                    | None.                                                                                                            |
| `WorkstationSidebarConnector/index.tsx:578-603` | Active child selection                                 | keep with reason | Selection is derived from the canonical Kanban section and projects-view atoms, so the parent expands automatically and the highlighted child stays synchronized with content without parallel local state.                              | None.                                                                                                            |
| `WorkstationSidebarConnector/index.tsx:550-728` | Work Items toggle and integrated item list             | fix              | Expanding Work Items replaces drafts/sessions with the existing project/work-item menu collection directly below the pinned group; collapsing restores sessions. The project list keeps normal row alignment.                            | None.                                                                                                            |
| `WorkManagement/index.tsx:75-108`               | Kanban/List/Diary switch in 40px header                | fix              | View mode is content-level chrome and consumed an entire sidebar section. `FactoryViewPill` is the existing design-system switch for these views and fits the established workstation header.                                            | Render the existing pill in the leading header slot and retain the detail back control in that slot when needed. |
| `WorkManagement/index.tsx:50-72,110`            | Full-width Kanban content                              | fix              | The nested `WorkStationShell` created a second resizable sidebar inside an app that already has a global navigation sidebar, reducing available content width and duplicating sidebar behavior.                                          | Remove the nested shell/sidebar and let the active surface fill the content region.                              |
| `GitHubWorkItemsSurface.tsx:2195-2251`          | GitHub issue preset filter                             | keep with reason | Removing the inner sidebar must not remove Assigned to me, Created by me, Closed, and All. The existing design-system `Dropdown` and `Button` preserve those controls in the issue toolbar with a visible selected label.                | None.                                                                                                            |
| `ChatPanelTabBar.tsx:105-160,255-268`           | Management tab title, icon, and plus-menu destination  | fix              | The chat pane previously exposed the same surface as both “Kanban” and “Ops Center.” The tab now uses the selected destination’s localized label and matching Lucide icon, while the plus menu consistently opens Kanban.                | None.                                                                                                            |
| `chatPanelTabDisplay.ts:8-55`                   | Section-aware chat-tab naming                          | fix              | The shared title resolver previously discarded the active section and always rendered “Kanban.” Mapping the canonical section to Kanban, Projects, GitHub Issues, or GitHub PRs keeps the singleton tab coherent as its content changes. | None.                                                                                                            |

Verdict counts: **fix 6**, **keep with reason 3**, **abstract 0**.

Accessibility check: the expandable Work Items row toggles from the full row with Enter/Space support; child and top-level rows retain shared navigation behavior; the header switch and chat tabs retain `TabPill` tab semantics and localized title attributes; the GitHub filter trigger is a labeled button backed by the shared keyboard-enabled dropdown. No arbitrary colors, shadows, or new interaction primitives were introduced.

Systematic sweep note: nested Kanban sidebar state, responsive collapse logic, portal-only GitHub filter chrome, and their re-export chains were swept and removed. No config-level visual sweep candidate remains.

Validation note: Prettier, targeted ESLint, the full TypeScript check, and targeted navigation/Kanban tests pass. The documented `frontend-ui-audit` skill file was unavailable at both listed paths, so this report follows the repository's current audit report format and decision conventions.
