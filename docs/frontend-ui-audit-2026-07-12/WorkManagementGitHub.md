# Kanban GitHub surfaces — frontend UI audit

Scope: the new GitHub Issues and GitHub PRs Kanban destinations, their filter/list layout, and in-place detail views.

| Line                                   | Element                              | Verdict          | Reason                                                                                                                                                                                            | Suggested change |
| -------------------------------------- | ------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `WorkManagementSidebar.tsx:204-232`    | GitHub Issues / GitHub PRs rows      | keep with reason | Reuses the existing `TreeRowBase`, Lucide icon set, row sizing, active-state behavior, and localized labels used by the rest of the Ops sidebar.                                                  | None.            |
| `WorkManagementSidebar.tsx:298-313`    | GitHub section navigation            | keep with reason | Both entries use the canonical singleton Kanban tab action and only change its typed inner section, so navigation and selection cannot drift.                                                     | None.            |
| `GitHubWorkItemsSurface.tsx:712-733`   | Visible secondary filter options     | keep with reason | Uses native buttons with existing fill/text tokens and a focus-visible ring. Keeping these options visible matches the requested list-of-options treatment better than the former icon-only menu. | None.            |
| `GitHubWorkItemsSurface.tsx:735-771`   | Repository selector                  | keep with reason | Reuses the design-system `Select` with search, small sizing, and matching dropdown width; no parallel selector primitive was introduced.                                                          | None.            |
| `GitHubWorkItemsSurface.tsx:773-920`   | Issue result rows                    | keep with reason | Preserves established issue state, label, metadata, context-add, browser, and My Station affordances with native button semantics.                                                                | None.            |
| `GitHubWorkItemsSurface.tsx:922-976`   | PR result rows                       | keep with reason | Converts the former external-only anchor into an internal detail button while retaining the explicit add-to-agent action. External navigation remains available from the detail view.             | None.            |
| `GitHubWorkItemsSurface.tsx:1824-1839` | Issue second-level view              | keep with reason | Reuses the existing issue detail panel and renders it inside the right content pane, leaving repository/filter context visible on the left.                                                       | None.            |
| `GitHubWorkItemsSurface.tsx:1841-1919` | PR second-level view                 | keep with reason | Adds a compact in-place detail with back, context, GitHub, branch, state, update, and draft information using existing Button and surface tokens.                                                 | None.            |
| `GitHubWorkItemsSurface.tsx:1921-2066` | Filter-left / list-right composition | keep with reason | Establishes one fixed secondary filter rail and one flexible results/detail pane, with independent overflow ownership and a shared search toolbar.                                                | None.            |
| `GitHubWorkItemsSurface.tsx:1968-2014` | GitHub/create/refresh actions        | keep with reason | Uses existing small outline icon buttons and accessible labels; issue creation is intentionally absent from the PR view.                                                                          | None.            |
| `index.tsx:66-72`                      | Ops section renderer                 | keep with reason | Issues and PRs mount the same canonical GitHub surface with a typed scope, avoiding duplicated list/filter implementations.                                                                       | None.            |

Verdict counts: **fix 0**, **keep with reason 11**, **abstract 0**.

Accessibility check: new actions are native buttons, icon-only controls carry accessible labels, focus-visible styling is present, and selecting an issue or PR keeps the detail view in the same keyboard/navigation context. No new arbitrary colors, shadows, or one-off component primitives were added.

Visual verification note: the standalone webpack page reaches only ORGII's Tauri initialization probe and logs the expected missing-window metadata error outside the native host. TypeScript, targeted ESLint, and state-transition tests pass; final density should be smoke-tested in the Tauri app at normal and narrow Kanban widths.
