# Shared Account Details UI Audit

Scope: the reusable Key Vault account detail surface and its Chat launchpad integration. The configured `frontend-ui-audit` skill was not present in either documented skill location, so this report applies the repository's required output columns and a manual design-system/a11y consistency review.

| Line                                                                 | Element                      | Verdict          | Reason                                                                                                                                                                    | Suggested change                                                             |
| -------------------------------------------------------------------- | ---------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `src/modules/shared/keyVault/AccountInlineDetails.tsx:59`            | `AccountInlineDetails`       | abstract         | Settings and Chat need the same account metadata, quota, compatibility, and credential treatment. A single shared renderer prevents the two surfaces from drifting.       | Keep this component as the canonical account-detail body.                    |
| `src/modules/shared/layouts/blocks/InfoRow.tsx:11`                   | `InfoRow`                    | abstract         | The label/value row is a general layout primitive and uses semantic theme tokens instead of page-specific colors.                                                         | Keep it in shared layout blocks and consume it through the shared module.    |
| `src/modules/shared/layouts/blocks/InlineExpandedSplitCard.tsx:14`   | `InlineExpandedSplitCard`    | abstract         | The responsive two-column detail layout is used independently of Key Vault settings and contains no domain behavior.                                                      | Keep the primitive shared; domain content belongs in `AccountInlineDetails`. |
| `src/modules/shared/launchpad/components/LaunchpadDashboard.tsx:614` | Chat inline detail container | keep with reason | `InlineInfoCard` matches the launchpad's established inline expansion geometry; `bg-bg-2`, `border-border-2`, and the existing radius remain theme-aware.                 | No change.                                                                   |
| `src/modules/shared/launchpad/components/LaunchpadDashboard.tsx:618` | Lazy detail fallback         | keep with reason | The standard `Placeholder` communicates both the deferred code import and the selected account refresh without introducing a second loading pattern.                      | No change.                                                                   |
| `src/modules/shared/launchpad/components/LaunchpadDashboard.tsx:641` | Key tile button              | fix              | The previously display-only key tile needed selected-state semantics after becoming interactive. `aria-pressed` and stable test IDs now expose the toggle state.          | Fixed in this change.                                                        |
| `src/modules/shared/layouts/blocks/InlineInfoCard.tsx:4`             | Inline card observability    | fix              | The asynchronously mounted shared card had no direct test hook. Optional `dataTestId` support makes the behavior observable without coupling callers to its internal DOM. | Fixed in this change.                                                        |
| `src/modules/shared/keyVault/AccountStatusIndicator.tsx:1`           | Footer status                | abstract         | Status label and color resolution must remain consistent between Settings and Chat.                                                                                       | Keep status mapping in this shared component.                                |
| `src/modules/shared/keyVault/AccountCompatibilitySection.tsx:40`     | Compatibility loading        | keep with reason | Compatibility RPCs begin only when the detail body mounts, satisfying the on-demand requirement; cleanup prevents updates after unmount.                                  | No change.                                                                   |

Verdict summary: **2 fix**, **2 keep with reason**, **4 abstract**. One fix is the interactive tile semantics above; the second is the new optional `dataTestId` support on `InlineInfoCard`, which makes the asynchronously mounted surface observable without changing visual behavior.

No multi-file design-token sweep candidate was found. The extracted components use existing semantic tokens and existing shared primitives.
