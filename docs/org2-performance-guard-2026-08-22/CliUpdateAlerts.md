# CLI update alerts performance guard

| Area               | Verdict | Evidence                                                                                                                                              | Change or reason kept                                                                                                                 | Verification                                                                                                           |
| ------------------ | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Background work    | fix     | Version checks are demand-driven; snooze expiry is coordinated by `useCliVersions` with one shared timeout per CLI and subscriber cleanup.            | No polling. Turning alerts off prevents initial/manual scans and unsubscribes the pending expiry timer.                               | Hook tests cover the off gate and cleanup; coordinator tests prove two subscribers share one timer and one forced RPC. |
| Memory             | keep    | Version snapshots, scheduled rechecks, in-flight state, handled deadlines, and persisted suppressions are all keyed by the finite `CliAgentType` set. | Existing 12-hour freshness remains; each CLI owns at most one entry in each structure.                                                | Type-level keying plus focused coordinator tests.                                                                      |
| Scope/isolation    | keep    | Suppression, timer, and single-flight request keys all use the selected `CliAgentType`.                                                               | Closing one CLI cannot suppress or schedule another CLI.                                                                              | Pure suppression tests and exact `codex` request assertions.                                                           |
| Rendering/hot path | keep    | No interval or render-loop work was added. Listeners wake only when a scan publishes or a scheduled recheck settles.                                  | A successful six-hour recheck may produce two bounded notifications so the alert cannot flash stale data while the scan is in flight. | Focused tests, ESLint, and full TypeScript check passed.                                                               |

Performance verdict: pass
